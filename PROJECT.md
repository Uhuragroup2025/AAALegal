# PROJECT.md — AAA Estudio Legal · Rediseño Web

> Documento vivo. Última síntesis: 2026-08-21 (D80 Home § "Sectores de experiencia": los títulos de las 6 tarjetas bajan de `clamp(1.15rem, 1.6vw, 1.5rem)` a 1.3rem fijo; D79 Home § "Sectores de experiencia": se ensancha la columna derecha del encabezado para que quede más pareja en alto con la izquierda; se fusionan Financiero+Fintech y Salud+Cajas de compensación en 2 tarjetas — el sitio vuelve a 6 tarjetas en vez de 8, conservando los íconos de Financiero y Salud; D78 segundo rediseño de Filosofía: las 3 tarjetas de D75 no convencieron, se reemplazan por `.cierre-grid`/`.cierre-card` (foto + tarjeta sólida con ícono), inspirado en una referencia externa pero con nuestra paleta/tipografía; se corrige el tamaño del H1 de Quiénes somos/Trabaja con nosotros a 48px, igual que Inicio/Metodología, fijando `font-size` en la clase base `.hero__title`; D77 se agrega el énfasis en cursiva dorada (`.text-gradient`) a los heroes de Quiénes somos y Trabaja con nosotros, que no lo tenían — criterio de estilo para el hero de cualquier página futura; en Metodología, "su crédito?" se une con nbsp para quedar siempre en el mismo renglón; D76 se quita el aviso de founders pendientes en "¿Quiénes somos?"; se normaliza `.text-gradient` en todo el sitio al color dorado sólido de Home (D26), quitando el degradado que caía en Metodología/Captación B2B por no tener la clase `.home-page` — nunca aprobado por el cliente; D75 Quiénes somos: "¿Quiénes somos?" recicla el diseño de Home § "¿Qué hace AAA?" para corregir el desajuste de márgenes; "Filosofía" pasa de texto plano a 3 tarjetas de atributo (`.philosophy-grid`); se retira el cierre de LinkedIn/Instagram; D74 corrige D73: el cierre de Servicios no coincidía visualmente con el de Home — se crea el modificador `.cierre-grid--global` (degradado, sin halo, botón dorado, h2 grande) aplicado a Home y Servicios, sin tocar la tarjeta de cierre propia de Captación B2B; D73 Servicios § "Transparencia" reusa el diseño de Home § "¿Qué hace AAA?" (dos columnas, fondo oscuro); se quita "¿Su empresa tiene un crédito en riesgo...?" y se reemplaza por el cierre real de Home, "Conversemos sobre su caso."; D72 nueva página `trabaja-con-nosotros.html`: "Nuestro equipo" y "Postulación" se mueven ahí desde `nuestro-equipo.html`, sin cambios de copy — el 4º ítem del menú deja de ser un ancla y pasa a apuntar a la página real; header/footer idénticos a las otras 6 páginas (D55/D67), enlaces actualizados en las 7; D71 corrige D70: el `min-height` no tenía efecto real porque `.media-placeholder` (clase base compartida) fija `aspect-ratio:4/5` con más peso — se agrega `aspect-ratio:1/1` a `.service-block__media`, que ahora sí controla el alto (320×320px en vez de 320×400px); D70 Servicios § "Nuestros servicios": el recuadro de imagen de los 3 bloques baja de 280px a 220px de alto mínimo, aceptando que quede cuadrado — se veía demasiado alto, desperdiciando viewport; D69 Servicios § "Nuestra metodología": la descripción de los 3 pasos del flujograma queda siempre visible en desktop (antes solo con hover), y los nodos se alinean con el margen izquierdo del eyebrow/título en vez de centrarse; D68 corrige un bug de D67: el heading "Sitio" del footer se veía gigante solo en Home (atrapado por una regla genérica `.home-page section h2` pensada para títulos de sección reales) — se excluye `.home-footer__section` de esa regla, sin tocar el resto de títulos de Home; D67 Footer: se agrega la columna "Sitio" (Inicio, Metodología, Quiénes somos, Trabaja con nosotros, Blog, Contacto) entre Contacto y Redes sociales — footer de 3 a 4 columnas — y se replica idéntico en las 7 páginas reales, mismo patrón que el header en D55, reemplazando el footer simple de las 6 páginas interiores; nuevo modificador `.site-footer--global`; D66 "En cifras": se quita la leyenda "Cifra de ejemplo — pendiente de dato aprobado por el cliente" bajo cada número — confirmado con el Owner que entiende que los 3 valores siguen siendo de ejemplo sin dato real aprobado, ver Decisiones — y se agrega un ícono por tarjeta (carpeta / persona / flecha ascendente) encima del número; D65 "¿Por qué AAA?": se revierte D64 — las cards vuelven a rectangulares (alto de card 260px/240px/230px→150px en las 3 vistas) y se quita el `max-width:13ch` del título (D45/D46), que dejaba un vacío a la derecha del texto; los colores de D64 no cambian; D64 "¿Por qué AAA?": las 6 tarjetas pasan a formato casi cuadrado (alto de card 160px→260px/240px/230px según viewport) y la fila de abajo invierte el orden de color respecto a la de arriba (Seguridad = azul oscuro, Compliance = azul claro, Cobertura nacional = blanco) — reemplaza el ciclo automático de color de D51 por selectores explícitos por posición; D63 Femcristar pasa de Solidario a Financiero, instrucción directa del Owner; D62 Sectores de experiencia: se crean 2 sectores nuevos ("Construcción e inmobiliario", "Comercio y consumo") para los 5 logos que no encajaban bien en los 6 sectores originales — el sitio pasa de 6 a 8 sectores, párrafo de intro actualizado; D61 los logos bajan de 76px a 75px, corrección de un valor de más; D60 Sectores de experiencia: los logos suben otro poco, de 64px a 76px de alto — ajuste fino tras D59; D59 se quita la línea divisoria entre header y logos, y los logos suben de 40px a 64px de alto — se veían muy pequeños incluso en desktop; D58 Sectores de experiencia: los 15 logos reales de clientes actuales de AAA (aportados por el Owner) reemplazan los 12 chips placeholder de D56 — mapeo sector↔cliente confirmado por el Owner para 8, y delegado a nuestro criterio para 6 sin encaje sectorial obvio (Neat, Nuestro Fondo, Constructora Meléndez, El Gran Langostino, Ma. Isabel, K-Listo); archivos movidos a `assets/img/` plano, `.sector-card__logo` pasa de caja-con-texto a solo imagen; D57 el header (logo/menú) queda siempre alineado con el texto del hero/título de cada página, en cualquier ancho de pantalla — header pasa a usar `--container-padding`/`--container-max` en vez de una escala propia; el hero de foto completa de Servicios/Contacto también se acota a `--container-max` (con confirmación explícita del Owner, ya que la foto deja de llegar a los bordes en pantallas muy anchas), Home no se ve afectada; D56 Sectores de experiencia: cada chip de logo lleva una marca abstracta genérica de ejemplo junto al texto — sin inventar clientes reales, mismo criterio que D38; D55 header full-width de Home replicado idéntico en las 7 páginas reales — reemplaza la píldora flotante de las 6 páginas interiores, revierte esa parte de D34, nuevo modificador `.site-header--global` scoped al `<header>`; D54 Sectores de experiencia: ícono + nombre centrado en cada tarjeta; D53 Home § "¿Por qué AAA?": se reduce el espacio en blanco de cada tarjeta — ícono y título agrupados arriba en vez de estirados a los extremos, alto de card 220px→160px en las 4 vistas responsive; D52 mismo módulo: en desktop, la grilla pasa de 6 columnas en una sola fila a 3+3 — el título más largo no cabía sin desbordarse y bajar más la fuente no era opción; D51 mismo módulo: reorden completo y ampliación de 5 a 6 atributos — Infraestructura para atención masiva, Equipo especializado, Tecnología (ADY), Seguridad de la información, Compliance, Cobertura nacional; se retira "Representación exclusiva del acreedor" — ver Decisiones). Previo, 2026-08-20: D50 flujograma de "Nuestra metodología": en mobile/tablet se quita la línea divisoria horizontal entre pasos y se reemplaza por la misma flecha de desktop, rotada 90° apuntando hacia abajo; D49 flujograma de "Nuestra metodología": en mobile/tablet (≤800px) el ícono pasa al lado izquierdo del título en vez de encima, para ahorrar espacio vertical — solo esa vista, sin cambios en desktop; D48 Servicios § "Nuestra metodología": rediseño a flujograma horizontal de 3 nodos conectados, sin scroll — pedido nuevo del cliente, confirmado explícitamente que no repite el flujograma rechazado en D32/D33; descripciones solo con hover, mismo patrón de D47; reemplaza D33 solo en esta sección; D47 Home § "Cómo intervenimos": la descripción de cada bloque solo aparece con hover, complementando el efecto ya existente — solo en dispositivos con hover real, siempre visible en touch/mobile; D46 "¿Por qué AAA?": se reduce el tamaño de los títulos de tarjeta de 1.5rem a 1.3rem, el texto quedaba casi pegado al borde inferior tras pasar a Krub bold en D45; D45 los títulos de las tarjetas en "Sectores de experiencia" y "¿Por qué AAA?" pasan de la serif de display a Krub bold; D44 hero de Home: el texto del botón único cambia a "Conozca cómo lo hacemos"; se reduce el espacio entre título y cards en Situaciones, solo en esa sección; D43 hero de Home: se quita el botón secundario "Conozca cómo lo hacemos" y el botón primario ahora hace scroll a "Cómo intervenimos" en vez de ir a la página de contacto; D42 el texto del botón destacado del header vuelve de "Contacto" a "Mi caso" en las 7 páginas — confirmado que apunta al flujo de la plataforma ADY, aún sin URL real; D41 contador animado "En cifras" bajo Sectores de experiencia, valores placeholder marcados como "cifra de ejemplo"; D40 espacio entre "Conozca nuestra experiencia →" y la foto en Dimensión humana; D39 corrige el desborde de "Sectores de experiencia" al margen total de la página, envolviendo la grilla en `.container` y agregando el mismo slide/carrusel móvil que ya usan Situaciones y Casos de éxito — ver Decisiones). Previo, mismo día: D38 Sectores de experiencia rediseñado: ícono/nombre + logos placeholder de clientes en la misma caja, grilla estática 3→2→1 en vez del marquee. Previo: D37 (Home: se quitan Logos clientes y las pills del equipo; el texto de Dimensión humana pasa antes que la foto), D35 (Sectores de experiencia antes que Casos de éxito), D36 (reestructuración del menú principal — Blog oculto y reemplazado por "Trabaja con nosotros", Servicios→"Metodología", Nuestro Equipo→"Quiénes somos", el botón destacado vuelve a decir "Contacto"), D34 (menú: sin selector de idioma, logo real unificado en las 7 páginas), D33 ("Nuestra metodología" en Servicios vuelve a su diseño original, sin scroll y con menos espacio — reemplaza D32), D31 (se retira CTA de texto redundante en Situaciones). Previo: 2026-08-13 (D30 tipografía Big Caslon/Krub del manual de marca, vía Libre Caslon Display + Libre Caslon Text + Krub, reemplaza el Visual Language Lab de D26); 2026-08-10 (D27 ronda de correcciones SEO en Homepage; D28 ajuste de eyebrow y registro tú/usted en Situaciones; D29 homogenización a "usted" en las 8 páginas del sitio); 2026-08-05 (D24 posicionamiento y tres macroservicios; D25 narrativa de Homepage; D26 Visual Language Lab).
> Fuente de verdad para alcance: **propuesta comercial**. El brief orienta objetivo/necesidad pero no amplía alcance por sí solo. Ver [AGENTS.md](AGENTS.md) para jerarquía completa de fuentes y [REFERENCES.md](REFERENCES.md) para insumos originales.

---

## Resumen

AAA Estudio Legal (antes "Agudelo Abogados Asociados") encarga un rediseño de su sitio web corporativo actual (agudeloabogados.com, WordPress + Elementor sobre LiteSpeed). No es un simple rediseño visual: es la materialización de un reposicionamiento de marca — de una firma percibida como generalista y con imagen de +5 años de antigüedad, hacia una firma más madura, tecnológica y especializada, operando bajo el nombre "Estudio" (adoptado en 2024) y el monograma AAA (nacido en 2020).

El proyecto fue vendido por Uhura Group mediante propuesta comercial de hasta 6 secciones, prototipo UX/UI previo a construcción, implementación en WordPress administrable, y dos gates formales de aprobación del cliente.

## Objetivo

Evolucionar la plataforma digital de AAA Estudio Legal perfeccionando su usabilidad, para reposicionar estratégicamente la marca proyectando una identidad corporativa más madura, tecnológica y humana (fuente: propuesta comercial, slide 3).

**Reemplazado (Decisión D15, Owner, 2026-07-23) — sustituye la lectura previa basada en el brief, no se elimina, se registra el cambio:**
1. **Nivel principal — Reconocimiento de marca y showcase de servicios**: que el mercado (clientes potenciales, aliados, mercado en general) reconozca a AAA Estudio Legal y comprenda su oferta de servicios.
2. **Nivel secundario — Atracción de talento** ("Talento AAA"): pasa de principal a secundario. Sigue siendo un objetivo del sitio, ya no el que determina la acción central.

*(Lectura anterior, ya no vigente pero conservada por trazabilidad: "Nivel principal — Atracción de talento... Nivel secundario — Confianza del cliente", basada en una inferencia del brief original. D15 la reemplaza explícitamente.)*

**✅ Cerrado (Decisión D17, Owner, 2026-07-23).** La tensión entre D8 (talento como acción de conversión principal) y D15 (talento no es el objetivo principal) queda resuelta: D17 fija que la conversión principal es captar empresas; el formulario de talento (D8) es secundario y estrictamente contextual. Ver [Conversión](#conversión) y STATUS.md → contradicción #6 (resuelta).

## Audiencia

**Actualizado (D15):** dado que el objetivo principal pasa a reconocimiento de marca/servicios, la audiencia primaria se reordena en consecuencia:

- **Primaria: mercado general y clientes potenciales/corporativos** (incluye clientes con procesos de insolvencia y entidades públicas/financieras) que necesitan reconocer la marca y validar su oferta de servicios, solidez y seguridad de la información (ISO 27001, brief/brochure).
- **Secundaria: talento profesional potencial** (candidatos con perfil jurídico de alto nivel) — pasa de primaria a secundaria (D15).
- Nota: el sitio actual está en español, con habilitación de inglés vía traducción automática (Polylang) — no se construye contenido nativo en inglés (propuesta, slide 9).

## Conversión

**Reemplazado (Decisiones D17 y D18, Owner, 2026-07-23) — cierran las incidencias de jerarquía de conversión y función de ADY. Reemplazan cualquier interpretación anterior contradictoria (D9, D10, D13, D16). Historial conservado abajo, no eliminado.**

**D17 — Jerarquía de conversión:**

Acción de conversión **principal**: captar **empresas** que necesiten los servicios jurídicos de AAA Estudio Legal. El formulario/flujo principal está dirigido a empresas interesadas en consultar o contratar los servicios de la firma (contenido de este formulario: D9, ahora elevado a principal).

Acción **secundaria y contextual**: el formulario de **AAA Talento** (D8). Su única función es recibir postulaciones y hojas de vida. **No debe tratarse como** conversión principal, CTA global, objetivo central del sitio, ni como formulario equivalente al de empresas. Debe aparecer **únicamente** en el contexto de la página Talento AAA o de contenido sobre oportunidades profesionales — nunca en header, footer, u otras páginas como CTA site-wide.

**D18 — Función de ADY:**

ADY representa la tecnología, innovación y robustez de AAA Estudio Legal, y funciona como el **mecanismo destacado de la conversión principal** mediante el CTA **"Consulta tu caso aquí"**. Su comportamiento debe transmitir la claridad e inmediatez de un botón de compra de e-commerce — sin representar ni sugerir una compra, carrito o pago. Dirige a una **URL externa** (a compartir más adelante) que inicia o continúa el flujo principal de consulta. Reemplaza la definición previa de D10/D13 (botón discreto de solo-login sin explicar qué es ADY) — ahora es un CTA prominente, no discreto.

**Requisito obligatorio de C2 (prototipo), registrado por D18:** el CTA de ADY debe, en el prototipo:
- ser claramente accionable (visual y funcionalmente);
- funcionar en desktop y mobile;
- contemplar estados normal, hover, focus y activado;
- ser accesible (teclado, lector de pantalla, contraste AA mínimo).

**Pregunta abierta (no se resuelve aquí):** si el formulario nativo de empresas (D9/D12, entrega por correo sin CRM) y el CTA de ADY hacia la URL externa son el **mismo flujo** o **dos entradas distintas** al mismo objetivo. Se deja para 03A/C2 cuando se conozca la URL externa.

**Historial de la jerarquía (nada se borra, D17/D18 reemplazan el orden vigente):**
- 2026-07-17: D8 (talento=principal) > D9 (B2B=secundaria) > D10 (ADY=terciaria).
- 2026-07-23 (mañana): D15 invierte el objetivo de fondo (talento ya no es principal); D16 reconcilia: reconocimiento de marca/servicios (principal) > talento (D8, secundaria) > B2B (D9, terciaria) > ADY (D10/D13, cuaternaria — solo login, sin explicar).
- 2026-07-23 (esta actualización): D17/D18 reemplazan D16 — empresas/B2B (D9, principal, con ADY como su mecanismo destacado vía D18) > talento (D8, secundaria y contextual, nunca global). El objetivo de marca general (D15) no se toca — sigue siendo el objetivo del *sitio*; D17 es específicamente sobre la jerarquía de *conversión*.

**Límite técnico (no negociable sin Change Request):** los formularios de empresas y talento caben en el presupuesto contractual de "hasta 3 formularios, envíos ilimitados". Cualquier integración de esos leads con CRM/ERP (enrutamiento automático, calificación, sincronización) está **explícitamente fuera de alcance** (propuesta, slide 7: "Integraciones ERP/CRM") — se recibirían como envíos de formulario, no como pipeline comercial integrado, salvo Change Request.

**Confirmado (D12, Owner, 2026-07-17):** los leads (talento y empresas) llegan por **notificación de correo con opciones nativas de WordPress** — sin CRM. Qué plugin/mecanismo concreto de formularios se usa es una decisión de **03A — Arquitectura WordPress**, no se define aquí.

**Página dedicada para captación de empresas (D11):** el Owner autorizó explorar una **página nueva y dedicada** para esta acción, ahora principal. Esto suma una **8ª página** al sitemap (6 vendidas + página legal ya autorizada como adicional [D2] + esta). **No queda contratada por esta decisión** — requiere confirmación de Product Lead sobre fricción contractual con Uhura, y muy probablemente **Change Request**. Nombre/contenido de esta página: pendiente.

## Alcance

**Fuente que manda: propuesta comercial.** Incluye:
- Hasta 6 secciones/páginas (ver Sitemap) **+ 1 página legal adicional, no contabilizada dentro de las 6** (Decisión D2)
- Prototipo UX/UI navegable (generado con IA, usando contenido actual) — Gate 1
- Implementación en WordPress: homepage custom + páginas internas administrables
- Custom Post Types: **no se definen en esta fase** — hasta 2 CPT, hasta 6 campos c/u, disponibles como techo contractual; su necesidad/estructura/destino se decide en **03A — Arquitectura WordPress**, priorizando páginas/posts/bloques nativos antes de crear CPTs (Decisión D3)
- Hasta 6 banners web
- Hasta 3 formularios (envíos ilimitados)
- SEO + GEO (motores de búsqueda y motores de IA) + performance
- UX Writing: reescritura completa del copy en tono dinámico, serio y sofisticado (no migración del contenido actual)
- Redacción inicial de hasta 2 entradas de blog (modelo para seguir alimentando)
- Migración de hosting a infraestructura cloud (hosting recomendado: Hostinger WordPress CMS Plan, ~$70 USD/mes, 4 años, incluye SSL)
- Traducción automática ES→EN vía Polylang (gestión de URLs/hreflang automática, sin costo)
- Analítica: GA4 + GTM, eventos principales, seguimiento de conversiones
- Capacitación básica de administración del sitio (1 hora)
- QA en capas (técnico / visual / funcional) + 1 ronda de ajustes post-QA cliente

**Explícitamente fuera de alcance (propuesta, slide 7 y 17):**
- Integraciones ERP/CRM
- Funcionalidades a medida complejas
- Rediseños posteriores al Gate 1 (cambios estructurales post-gate → Change Request, se cotiza aparte)
- Copy extenso (⚠️ ver contradicción en STATUS.md respecto a "UX Writing" completo)
- Compra de banco de imágenes, fotografía o producción de video
- Compra de plugins o suscripciones adicionales no listadas en costos operativos
- Mantenimiento posterior al lanzamiento (salvo plan adicional)

**Resuelto (Decisión D2, Owner, 2026-07-17):** la página legal es obligatoria, se autoriza como página adicional **no** contabilizada dentro de las 6 páginas principales. Debe usar una plantilla sencilla y reutilizable. La IA no redacta ni valida contenido legal — se usa únicamente texto entregado o aprobado por el cliente. *(Nota para Product Lead: esto añade una 7ª página respecto a la lectura literal de "hasta 6 secciones" de la propuesta — confirmar que no genera fricción contractual con Uhura; no bloquea C0.)*

## Sitemap

Según propuesta comercial (slide 6), el mapa de sitio vendido — y que se interpreta como el tope de "hasta 6 secciones" — es:

1. **Inicio (Home)**
2. **Talento AAA** — página vendida (propuesta, slide 6); **objetivo secundario del sitio** (D15) y formulario secundario/contextual (D17, no CTA global). Narrativa inspiracional, visión de la firma contada a través de Julián Agudelo (socio cofundador) como eje narrativo. Copy candidato en [content/05-talento-aaa.md](content/05-talento-aaa.md).
3. **Capacidad Técnica (Servicios)** — alta profundidad técnica, acotada a tres macroservicios: Defensa de acreedores en procesos de insolvencia / Normalización de Activos / Derecho Corporativo. Derecho Concursal deja de ser un bloque independiente y se integra en el primer macroservicio (D24). Sostiene la conversión principal (D17). Copy candidato en [content/02-capacidad-tecnica.md](content/02-capacidad-tecnica.md).
4. **Nuestro Equipo** — módulos con perfiles "con altura": Julián Agudelo, Angélica Chamorro (socios), Jaime Marmolejo (director); incluye la sección "Quiénes somos" (estructura D19, copy candidato en [content/04-nuestro-equipo.md](content/04-nuestro-equipo.md)).
5. **Nuestra Experiencia (Clientes)** — hasta 4 casos de éxito, sectores: Financiero, Fintech, Solidario, Organismos Públicos. Copy candidato (casos en placeholder) en [content/03-nuestra-experiencia.md](content/03-nuestra-experiencia.md).
6. **Noticias y Análisis (Blog)** — publicaciones/análisis jurídico; single page listado + hasta 2 entradas iniciales (autoría **pendiente**, ver STATUS.md). Copy de encabezado en [content/07-noticias-analisis.md](content/07-noticias-analisis.md).
7. **Página Legal** — adicional, fuera del conteo de las 6 anteriores; plantilla simple y reutilizable; contenido provisto/aprobado por el cliente, no redactado por la IA (Decisión D2). Sin outline de copy — ver [content/08-pagina-legal.md](content/08-pagina-legal.md).
8. **Captación de empresas (insolvencia)** — página nueva y dedicada, **no contratada todavía**; pendiente de confirmación de Product Lead y probable Change Request (Decisión D11). Es la **acción de conversión principal** (D17). Copy candidato en [content/06-captacion-b2b.md](content/06-captacion-b2b.md).

Elemento transversal (no es página propia): **Acceso a ADY** — mecanismo destacado de la conversión principal, CTA "Consulta tu caso aquí" (Decisión D18, reemplaza D10). Especificación en [content/09-ady-cta-c2.md](content/09-ady-cta-c2.md).

## Narrativa

- **Eje narrativo humano:** Julián Agudelo (cofundador) como figura central de la historia de Talento AAA. *(Nota: Brigard Urrutia usa la línea "aquí empieza tu historia con nosotros" como su propio tagline — es solo referente de dirección, no se copia literalmente; el copy candidato en content/05 usa una frase propia.)*
- **Especialización, no generalismo:** el sitio actual y el brochure exhiben una oferta amplia de áreas de práctica (14 áreas en el sitio vivo: concursal, financiero/fiduciario, contractual, corporativo, competencia y consumo, inmobiliario, público, disciplinario, patrimonio, litigios especiales, economía solidaria, asesoría empresarial, controversias comerciales, normalización de activos). D24 actualiza y reduce el relato a tres macroservicios: Defensa de acreedores en procesos de insolvencia, Normalización de Activos y Derecho Corporativo. Derecho Concursal se conserva como vocabulario técnico dentro del primer macroservicio, no como bloque independiente. El brochure es fuente de contenido existente, no narrativa aprobada — debe filtrarse, no migrarse íntegro.
- **Tres valores rectores** (brief, pregunta 3): (1) innovación con rigor — ideas jurídicas distintas sostenidas en análisis profundo; (2) solidez con tecnología — estabilidad estructural con la tecnología como eje; (3) dimensión humana — "somos una empresa de personas". Tono transversal que une los tres: **dinamismo**.
- **Seguridad de la información como mensaje sutil de marca:** alineación con ISO 27001, firewalls, copias de respaldo — relevante para clientes corporativos/públicos (brief pregunta 8, reforzado por brochure).
- **Tono de copy:** dinámico, serio y sofisticado — reescritura completa, no migración (brief pregunta 5). **Resuelto (Decisión D5, Owner, 2026-07-17):** no existe una regla previa de "copy corto" o "copy extenso" como categoría fija; la extensión de cada bloque se define según narrativa, storytelling, intención, claridad y escaneabilidad — evitando relleno y texto artificialmente largo. Content hará QA de esto posteriormente.

## Marco técnico

- **Plataforma:** WordPress, administrable por el cliente (no headless, no funcionalidades a medida).
- **Stack actual confirmado:** WordPress + Elementor sobre LiteSpeed (brief, pregunta 8). El cliente da libertad total para elegir plataforma/herramientas, con una única expectativa de fondo: el sitio debe sentirse rápido, moderno y bien construido.
- **Hosting recomendado:** Hostinger — plan WordPress CMS, soporte global, CMS, SSL, contrato de 4 años, ~$70 USD/mes (propuesta, slide 12).
- **Traducción:** Polylang (plan automático, gratuito), gestión de URLs y hreflang editable desde WordPress.
- **Formularios:** hasta 3, envíos ilimitados, sin costo adicional declarado.
- **Analítica:** GA4 + GTM, eventos principales y seguimiento de conversiones. **+ Microsoft Clarity (D20)**, complemento gratuito para heatmaps/session replay, implementado vía el mismo contenedor GTM — sin plugin nuevo, sin costo. Requiere aviso de cookies/privacidad que lo cubra (a incorporar en el banner de consentimiento del sitio).
- **CPTs:** techo contractual de hasta 2, hasta 6 campos cada uno — **no se definen en esta fase** (Decisión D3). Se deciden en 03A — Arquitectura WordPress, priorizando páginas/posts/bloques nativos antes de crear CPTs.
- **Referencia a ADY:** CTA destacado ("Consulta tu caso aquí", D18) que dirige a una URL externa (a compartir) — sin integración técnica real, sin cambio de alcance técnico. Requisitos de interacción obligatorios para C2 en Decisiones → D18.
- **Seguridad:** sin requerimiento técnico explícito más allá de lo estándar de hosting/SSL; el mensaje ISO 27001 es de posicionamiento de marca, no necesariamente una implementación técnica nueva en el sitio.
- **Interacciones de scroll (scrollytelling, sticky crossfade, parallax):** no se definen en esta fase (Decisión D7). Se definen en **C2** según propósito narrativo, accesibilidad, rendimiento y factibilidad en WordPress; deben usar JavaScript mínimo, progressive enhancement, fallback funcional sin JS, y respetar `prefers-reduced-motion`.
- **Moneda del proyecto:** COP — pesos colombianos (Decisión D4). El sitio opera en Colombia.

## Decisiones

> Registro de decisiones tomadas. Ninguna decisión de diseño, alcance ampliado o interpretación de contradicción debe asumirse aquí sin registrar su origen (usuario, Product Lead o cliente).

| # | Decisión | Origen | Estado |
|---|----------|--------|--------|
| D0 | La propuesta comercial es la fuente que manda sobre alcance vendido; el brief no amplía alcance por sí solo | Instrucción del Owner (B00) | Vigente |
| D1 | El manual de marca define restricciones de identidad (logo, color, tipografía) pero no reemplaza el trabajo de diseño; hay libertad para modernizar sin sujeción literal | Brief, pregunta 2 | Vigente |
| D2 | Página legal obligatoria, autorizada como página adicional no contabilizada dentro de las 6 principales; plantilla simple y reutilizable; contenido provisto/aprobado por el cliente, no redactado ni validado por la IA | Owner (cierre de pendientes B00) | Vigente |
| D3 | Los CPT no se definen todavía; su necesidad, estructura y destino se deciden en 03A — Arquitectura WordPress, según el contenido real; priorizar páginas/posts/bloques nativos antes de crear CPTs | Owner (cierre de pendientes B00) | Vigente |
| D4 | Moneda del proyecto: COP (pesos colombianos) | Owner (cierre de pendientes B00) | Vigente |
| D5 | No hay regla fija de "copy corto" vs. "copy extenso"; la extensión de cada bloque se define por narrativa, storytelling, claridad y escaneabilidad, evitando relleno; Content hace QA de esto después | Owner (cierre de pendientes B00) | Vigente |
| D6 | Los vacíos del manual de marca no se resuelven inventando reglas de marca; en C1 se podrán proponer criterios candidatos, validados durante el prototipo — no son decisiones de fondo aprobadas todavía | Owner (cierre de pendientes B00) | Vigente (de proceso, no de contenido) |
| D7 | Las interacciones de scroll (scrollytelling, sticky crossfade, parallax) no se definen en B00; se definen en C2 según propósito narrativo, accesibilidad, rendimiento y factibilidad en WordPress, con JS mínimo, progressive enhancement, fallback y `prefers-reduced-motion` | Owner (cierre de pendientes B00) | Vigente |
| D8 | Formulario de captación de talento en Talento AAA (capacidades/experiencia/trayectoria); campos exactos pendientes de C1/prototipo | Owner (Sesión 01) | Vigente como contenido — **estado de rango: ver D17 (secundario y contextual, no global)** |
| D9 | Captación de empresas/leads B2B de clientes con insolvencia — invierte la lectura previa basada en el brief ("no es lead comercial directo") | Owner (Sesión 01) | Vigente; **elevado a acción de conversión principal por D17** |
| D10 | Acceso a ADY se presenta como botón/CTA visible (no solo referencia pasiva); sin integración real, sin cambio de alcance técnico | Owner (Sesión 01) | **Reemplazado por D18** (ADY pasa de CTA discreto a mecanismo destacado de conversión) |
| D11 | Se autoriza explorar una 8ª página dedicada para captación de empresas, fuera de las 6 vendidas y de la página legal (D2) | Owner (Sesión 01) | **No contratada** — pendiente confirmación de Product Lead y probable Change Request |
| D12 | Los leads (talento y empresas) llegan por notificación de correo con opciones nativas de WordPress, sin CRM/ERP — dentro de lo excluido por la propuesta; el mecanismo/plugin concreto se decide en 03A | Owner (Sesión 01) | Vigente |
| D13 | El botón/CTA de ADY debe llevar directo al portal de login, sin explicar qué es ADY — solo transmitir que hay tecnología detrás (refina D10) | Brief visión de la firma AAA Abogados.docx (Sesión 01) | **Reemplazado por D18** |
| D14 | Dirección de oficina vigente y definitiva: Calle 28 Norte # 6BIS-26, Barrio San Vicente, Cali, Valle — cierra la contradicción de 3 vías (brochure "701", sitio actual "403", ambas descartadas) | Owner, 2026-07-23 | Vigente — cierra contradicción #4 (STATUS.md) |
| D15 | El objetivo principal **del sitio** es reconocimiento de marca y showcase de servicios; la atracción de talento **no** es el objetivo principal — pasa a secundario. No confundir con D17 (jerarquía de *conversión*, más específica) | Owner, 2026-07-23 | Vigente en Objetivo/Audiencia |
| D16 | Orden de conversión: reconocimiento de marca/servicios (principal) > talento (secundario) > B2B (terciario) > ADY (cuaternario) | Owner, 2026-07-23 | **Reemplazado por D17/D18** (mismo día, actualización posterior) |
| D17 | Jerarquía de conversión final: acción **principal** = captar empresas que necesiten los servicios jurídicos (formulario dirigido a empresas, D9 elevado); el formulario de AAA Talento (D8) es **secundario y estrictamente contextual** — no es CTA global, no es objetivo central, no equivale al formulario de empresas, y solo aparece en Talento AAA / contenido de oportunidades profesionales | Owner, 2026-07-23 (tarde) | Vigente — reemplaza D16 |
| D18 | ADY representa tecnología/innovación/robustez y funciona como el mecanismo destacado de la conversión principal, CTA "Consulta tu caso aquí", con claridad e inmediatez de botón de e-commerce (sin ser una compra); dirige a URL externa (a compartir) que inicia el flujo principal de consulta. Requisito obligatorio de C2: accionable, desktop+mobile, estados normal/hover/focus/activado, accesible | Owner, 2026-07-23 (tarde) | Vigente — reemplaza D10/D13 |
| D19 | Se crea la estructura narrativa de "quiénes somos" (fundadores Julián Agudelo y Angélica Chamorro) dentro de Nuestro Equipo, sin fusionarla con el eje narrativo de Talento AAA (solo Julián); el copy es placeholder hasta que el cliente lo cuente | Owner, 2026-07-23 (tarde) | Vigente — cierra contradicción #7 (STATUS.md) |
| D20 | Se adopta la propuesta de agregar Microsoft Clarity como complemento gratuito de GA4+GTM, vía el mismo contenedor de GTM ya contratado — sin plugin nuevo, sin costo, sin Change Request | Producer, propuesta adoptada por el Owner, 2026-07-23 (tarde) | Vigente — cierra contradicción #9 (STATUS.md) |
| D21 | Copy final aprobado para las 7 páginas con contenido (v1: Nuestra Experiencia, Talento AAA, Noticias y Análisis; v2 corregido por SEO: Inicio, Capacidad Técnica, Nuestro Equipo, Captación de empresas); se instruye avanzar a preparación de Gate 1 (prototipo). Los placeholders de contenido pendiente del cliente no quedan aprobados como finales por esta decisión | Owner, 2026-07-24 | Vigente |
| D22 | Tipografía de títulos: se aprueba **Space Grotesk** (alternativa "C" del comparativo `prototype/type-lab.html`) como fuente de encabezados (H1–H4, marca), reemplazando a Big Caslon. Krub se mantiene sin cambios en cuerpo de texto/UI. Aplica libertad de modernización ya reconocida por D1 (el manual de marca no se reemplaza, se moderniza su aplicación tipográfica). La dirección cromática candidata (§ prototype/README.md 1.2) sigue **sin decidir** — esta decisión es solo sobre tipografía | Owner, 2026-07-29 | **Reemplazado por D26** como dirección futura; permanece en la Home actual hasta implementar el Design Language aprobado |
| D23 | **Reemplazo de la paleta institucional.** Se sustituye el navy `#063B59` + gama dorada del manual de marca por una nueva paleta turquesa/teal: navy-deep `#04445F`, accent-main `#189AB4`, accent-light `#74E7DA`, surface-soft `#D4F2F4`, ink-dark `#101820`, surface-light `#F7F8F5`. A diferencia de D22 (modernización de aplicación tipográfica dentro de la libertad de D1), esto **no** es una modernización de aplicación — es un cambio de identidad de color que ya no corresponde a ningún valor del manual de marca. El Owner confirmó explícitamente reemplazarla (no conservarla como candidata), entendiendo que requiere registro como decisión y validación formal de Product Lead/cliente antes de Gate 1 — no queda aprobada como identidad de marca definitiva por esta sola instrucción de prototipo | Owner, 2026-07-29 | **Reemplazado por D26** como dirección futura; persiste únicamente en estilos heredados de la Home hasta su siguiente iteración |
| D24 | **Posicionamiento y portafolio de servicios.** AAA representa exclusivamente a acreedores en procesos de insolvencia; no asesora ni representa al deudor insolvente. El servicio antes presentado como “Insolvencia” pasa a denominarse **“Defensa de acreedores en procesos de insolvencia”**. Derecho Concursal deja de ser un macroservicio independiente y se integra como disciplina técnica dentro de ese servicio. El portafolio visible se reduce de cuatro focos a tres: Defensa de acreedores en procesos de insolvencia, Normalización de Activos y Derecho Corporativo. En Inicio se conservan sin cambios el H1, el subhead y la banda de credibilidad; el mosaico se titula “Tres focos, un mismo rigor técnico”. Los contenidos de hero de Servicios y Contacto se intercambian: Servicios recibe el posicionamiento “Soluciones para acreedores / ¿Un deudor puso en riesgo tu crédito?” y Contacto recibe “Soluciones jurídicas para proteger, normalizar y recuperar activos”. | Cliente (Julián), reunión reportada y aprobada por el Owner, 2026-08-04 | Vigente — reemplaza la estructura anterior de cuatro focos |
| D25 | **Iteración 2 — Narrativa de Homepage.** La Homepage adopta un recorrido de siete momentos: reconocimiento del problema → qué hace AAA → cómo interviene → razones para confiar → evidencia/casos de éxito → equipo → contacto. El hero se reemplaza por el problema explícito del acreedor y usa “Hablemos de su caso” como CTA a Contacto y “Conozca cómo lo hacemos” como CTA a Servicios. Se incorporan bloques de situaciones, definición de AAA, tres servicios, cinco razones de confianza y casos de éxito. Los casos, logos, testimonio y cifra se muestran únicamente como placeholders hasta recibir evidencia publicable del cliente. La sección de sectores se conserva sin cambios. El equipo usa una fotografía, texto corto, CTA y una banda animada de pills con los tres integrantes/cargos confirmados; no se inventan perfiles y las fotos individuales permanecen como avatares pendientes. El cierre pasa a “Conversemos sobre su caso”. No se crean archivos, carpetas, componentes ni cambios de arquitectura; se reutiliza el sistema existente de Homepage. | Owner, 2026-08-04 | Vigente en la Homepage candidata C2 — pendiente de QA y Gate 1 |
| D26 | **Visual Language Lab como sistema vigente.** La Product Lead aprueba la dirección visual general de `prototype/type-lab.html`. El sistema adopta Playfair Display para títulos y énfasis, Instrument Sans para lectura/UI, una paleta reducida alineada con marca (`#063B59`, `#B9CEED`, `#F5F3EE`, `#FFFFFF`, `#CAA181`, `#D9D9D9`, `#111820`) y una escala de espaciado `4/8/12/16/24/32/48/64/96/128`. Se conserva la estructura y el responsive existentes; no se crean dependencias, frameworks ni arquitectura nueva. **Migración ejecutada (2026-08-05):** aplicado a las 7 páginas construidas del prototipo vía `tokens.css`/`base.css`/`components.css` (compartidos por todo el sitio, no solo Home) y el `<link>` de Google Fonts en cada página. De paso se cerraron dos pendientes de refinamiento del laboratorio (degradados/iconografía, ver QA.md → D-10) y se corrigieron cuatro defectos encontrados durante la migración misma (ver QA.md → D-11 a D-14): texto ilegible en cards claras dentro de secciones oscuras, un asset SVG (mapa de sectores) con colores fijos de la paleta anterior, un anillo de foco de teclado invisible en el formulario de postulación, y una card de cierre que todavía tenía degradado. | Product Lead, 2026-08-05; migración ejecutada 2026-08-05 | Vigente e implementado en las 7 páginas del prototipo |
| D27 | **Ronda de correcciones SEO en Homepage.** Insumo: `inputs/resources/REVISION-2-COPYS-HOME.docx`. El documento presentaba, para 3 bloques, dos opciones de copy sin ganador marcado, y una sugerencia adicional que requería fabricar una cifra. El Owner resolvió cada punto explícitamente (no se asumió ninguno por defecto): (1) Hero H1+p → "Propuesta inicial" del documento ("Insolvencia y derecho corporativo: somos la voz del acreedor" + párrafo), sobre "Opción 2" (tuteo, habría requerido cambiar CTAs de todo el sitio) y sobre mantener el H1 anterior (abría con "deudor", señalado por SEO como potencialmente confuso); (2) H2 "Situaciones" → "¿Eres acreedor y te enfrentas a alguno de estos riesgos?" (Opción B del documento); (3) H2 "Cómo intervenimos" → "Soluciones jurídicas para proteger, normalizar y recuperar activos" (Opción A, coherente con los 3 servicios listados debajo — nota: esta misma frase ya aparece en el meta-description de `captacion-b2b.html`/Contacto por D24; es solapamiento leve de meta-description vs. H2 de otra página, no un H1/H2 duplicado, no se considera bloqueante). Además, se aplicó sin necesidad de elegir entre opciones el ajuste del párrafo de "Qué hace AAA" (repetía la idea del párrafo del hero). **Rechazado explícitamente:** la sugerencia de SEO de titular "Casos de éxito" con una cifra de ejemplo ("+50 casos...") — no hay cifra real aprobada por el cliente; contradice la regla ya vigente de no inventar métricas/casos (ver AGENTS.md → Prohibiciones, REFERENTS.md → Principios obligatorios). El H2 de esa sección se mantiene sin cambios. | Owner, 2026-08-10 | Vigente — aplicado en `prototype/index.html` |
| D28 | **Ajuste post-D27 en Home.** (1) Se retira el eyebrow "¿Se identifica con alguna de estas situaciones?" de la sección Situaciones — quedaba una pregunta justo encima de otra pregunta (el H2 de D27). (2) El H2 de esa misma sección pasa de tuteo a "usted": "¿Es acreedor y se enfrenta a alguno de estos riesgos?" (antes: "¿Eres acreedor y te enfrentas...?"), por consistencia de registro con el resto del copy de Home. **Nota de alcance más amplio, no aplicada por esta decisión:** el Owner señaló mezcla de tuteo/usted en el Home; al revisar el resto del prototipo se confirmó que el CTA global "Consulta tu caso aquí" (fijado literalmente por D18, aparece en el header de las 8 páginas) y varias piezas de `servicios.html`, `captacion-b2b.html` y `nuestro-equipo.html` usan tuteo. Homogeneizar el registro en todo el sitio es una decisión de mayor alcance (toca D18) que no se asume aquí — queda pendiente de que el Owner la autorice explícitamente. | Owner, 2026-08-10 | Vigente — aplicado en `prototype/index.html` |
| D29 | **Homogenización de registro a "usted" en todo el sitio.** El Owner confirmó extender el ajuste de D28 (solo Situaciones) a las 8 páginas del prototipo. Cambia: el CTA global "Consulte su caso aquí" (antes "Consulta tu caso aquí", fijado literalmente por D18 — este cambio actualiza esa literalidad, no la función/comportamiento del CTA); hero de Servicios "¿Un deudor puso en riesgo su crédito?" y su CTA de cierre "¿Su empresa tiene un crédito en riesgo...? / Consulte su caso con ADY" (wording literal de D24, actualizada aquí); Contacto/Captación B2B "Cuéntenos su caso y le contactamos con un diagnóstico inicial"; Nuestro Equipo/Postulación "¿Ve su carrera en AAA? Cuéntenos quién es usted.", botón "Postúlese" (antes "Postúlate", D8/D17), y la nota de envío del formulario; y el aviso de cookies ("Puede conocer más...") en las 8 páginas. content/*.md actualizado en paralelo para que la documentación no quede desincronizada del prototipo. Las decisiones D18/D24/D8/D17 no se eliminan ni se recontradicen en su función — solo se actualiza la literalidad del copy que citaban. | Owner, 2026-08-10 | Vigente — aplicado en las 8 páginas de `prototype/` y en `content/*.md` |
| D30 | **Tipografía: reemplazo del Visual Language Lab (D26) por la del manual de marca.** Títulos en Big Caslon, cuerpo/descripciones en Krub — alineado al manual de marca (D1), reemplaza Playfair Display + Instrument Sans (D26). Big Caslon no existe como webfont libre (fuente de sistema de macOS/iOS, ausente en Windows/Android); presentadas las opciones, el Owner eligió **Libre Caslon Display** como sustituto gratuito de la misma familia Caslon, con render idéntico en el 100% de navegadores — no es el archivo original, es una decisión explícita y documentada, no una aproximación silenciosa. Complicaciones técnicas resueltas: Libre Caslon Display solo trae el corte 400 (sin negrita real, sin itálica real) — se bajó `font-weight` de encabezados de 500/600 a 400 en `base.css`/`components.css` para evitar negrita sintética, y `.text-gradient` (usado para el énfasis en degradado dentro de H1) pasa a usar **Libre Caslon Text** (itálica real, misma familia) en vez de heredar una itálica sintética de Libre Caslon Display. También se quitó el tracking negativo calibrado para Playfair Display (D26) — un serif clásico como Caslon se lee mejor sin tracking apretado; criterio candidato, sujeto a QA visual. Verificado visualmente en las 8 páginas vía servidor estático local antes de publicar — sin negrita ni itálica distorsionada, sin regresiones de contenido. `prototype/type-lab.html` (laboratorio histórico de D26) se dejó sin tocar — sigue cargando Playfair Display/Instrument Sans a propósito, como registro de esa exploración pasada. | Owner, 2026-08-13 | Vigente — aplicado en las 8 páginas de `prototype/` |
| D31 | Se retira el CTA de texto "Si alguna de estas situaciones le resulta familiar, podemos ayudarle." (enlace a `captacion-b2b.html`) al final de la sección Situaciones en Inicio — quedaba redundante bajo el H2 en pregunta (D27/D28). No deja la página huérfana de acceso a Captación B2B: el CTA primario del hero ("Hablemos de su caso") ya enlaza ahí. | Owner, 2026-08-20 | Vigente — aplicado en `prototype/index.html` |
| D32 | **Rediseño de "Nuestra metodología" en Servicios, a flujograma sin scroll (pedido del cliente).** El cliente pidió reducir el espacio entre los módulos de la metodología y que los 3 pasos (diagnóstico, estrategia, representación) se vean como un flujograma sin necesidad de hacer scroll. Investigado antes de tocar código: esta sección **nunca existió en Home** (`index.html`/`content/01-inicio.md`) en el historial completo del repositorio (todos los commits, ambas ramas) — el Owner la buscó ahí por confusión. Vive en `servicios.html` (coincide textualmente con "Diagnóstico"/"Estrategia"/"Representación") y, con copy distinto, en `captacion-b2b.html` § "Nuestro proceso". El Owner confirmó aplicar el cambio **solo en Servicios**. Se retira el stepper con scroll (cada paso reservaba 50vh, activación por `IntersectionObserver` al cruzar el centro del viewport, columna sticky) y se reemplaza por 3 nodos conectados con flechas, visibles enteros sin scroll en desktop. Clases nuevas y **aditivas** en `components.css` (`.stepper__flow`/`.stepper__node`/`.stepper__connector`) — las clases del stepper original (`.stepper__grid`/`.stepper__intro`/`.stepper__steps`/`.stepper__step`) no se tocan y siguen intactas en Captación B2B, verificado que su comportamiento no cambió. Sin JS nuevo: los nodos son estáticos desde el primer render, no hay estado que observar. Verificado visualmente en desktop y mobile (conector rotado a vertical al apilarse) con servidor estático local antes de publicar. | Owner, 2026-08-20 | **Reemplazado por D33** — el Owner pidió revertir el flujograma; el diseño de nodos conectados no se conserva |
| D33 | **Reversión de D32: se conserva el diseño original de "Nuestra metodología" en Servicios, sin scroll y con menos espacio.** El Owner pidió deshacer D32 (no quería el flujograma de nodos) y en su lugar mantener el diseño de 2 columnas de 2026-07-31 (columna sticky + 3 pasos con icono/título/párrafo) — pero sin la activación por scroll (los pasos ya no se atenúan fuera de foco) y con el espacio entre pasos reducido. Se agregó el modificador `.stepper--compact` (nuevo, scoped a la sección de Servicios) en vez de modificar las clases compartidas — `.stepper__grid`/`.stepper__intro`/`.stepper__steps`/`.stepper__step` vuelven a ser exactamente las originales, así que Captación B2B § "Nuestro proceso" (mismo componente, sin el modificador) no se ve afectada; verificado explícitamente (min-height y atenuado por scroll intactos ahí). `.stepper--compact` sobrescribe `min-height: 50vh` → `0` y neutraliza el atenuado de pasos fuera de foco (`opacity/transform` forzados a su estado "visible" sin importar `.is-active`). De paso quedó documentado (no corregido, fuera de este ámbito) un bug preexistente: `.stepper__step` usa `padding-block: var(--space-10)`, token que no existe en la escala de `tokens.css` — la declaración original es inválida y no tiene efecto; sigue así en Captación B2B, no se tocó. No se modificó `js/main.js`: el `IntersectionObserver` que activa/desactiva `.is-active` sigue corriendo también sobre los pasos de Servicios, pero ya no tiene efecto visual dentro de `.stepper--compact`. Verificado visualmente en desktop y mobile con servidor estático local. | Owner, 2026-08-20 | Reemplazado por D48 — el cliente pidió un rediseño a flujograma para esta misma sección; `.stepper--compact` ya no se usa en `servicios.html` |
| D34 | **Header/menú: se quita el selector de idioma, se acorta el CTA a "Mi caso", y se unifica el logo real en las 8 páginas.** Tres pedidos del cliente/Owner sobre el menú principal: (1) quitar el selector "ES · EN" por ahora; (2) acortar el texto del botón de header de "Consulte su caso aquí" a **"Mi caso"**; (3) el Owner notó que el menú de Home es distinto al de las demás páginas y pidió aplicar los cambios en Home y replicarlos. Investigado antes de replicar: la diferencia no era solo "logo vs. texto" — Home usa una arquitectura de header completamente distinta (barra sólida de ancho completo, grid de 3 columnas) mientras las demás páginas usan la píldora flotante con efecto vidrio del sistema visual vigente (D26). Presentado esto al Owner con AskUserQuestion, **se descartó replicar la arquitectura completa de Home** — se mantiene la píldora flotante en las 6 páginas interiores (es el sistema visual actual) y solo se reemplaza el texto plano "AAA Estudio Legal" por el logo real (isotipo + wordmark SVG), dimensionado para esa píldora (más compacto que el de Home, que está pensado para una barra de 78px de alto). Sin animación de aparición del isotipo ni swap claro/oscuro del wordmark (ninguna página activa hoy `data-header-theme="light"`, queda dormido en ambos casos). Aplicado en las 7 páginas reales (Home + Servicios + Contacto + Nuestro Equipo + Blog + 2 artículos) — `proximamente.html` se dejó sin tocar a propósito, es un stub minimalista sin menú completo. Reglas CSS nuevas sin scope `.home-page`, así que Home conserva su propio tamaño de logo por especificidad de cascada, sin cambios. El botón "Consulte su caso aquí" del **cuerpo** de página (hero y cierre de Captación B2B) no se tocó — el pedido era sobre el botón del menú, no sobre CTAs de contenido. Verificado visualmente en las 7 páginas, desktop y mobile, sin errores de consola. | Owner, 2026-08-20 | **Parcialmente reemplazado por D36 y D55** — el texto del botón de header ("Mi caso") vuelve a "Contacto" (D36, luego de vuelta a "Mi caso" por D42); la arquitectura de "píldora flotante" en las páginas interiores queda reemplazada por el header full-width de Home (D55, 2026-08-21); el resto (sin selector de idioma, logo real unificado) sigue vigente |
| D35 | Se reordena Home: la sección "Sectores de experiencia" pasa a ubicarse **antes** que "Casos de éxito" (antes iba después). Solo cambia el orden de las dos secciones completas — ningún contenido dentro de ellas se modificó. | Owner, 2026-08-20 | Vigente — aplicado en `prototype/index.html` |
| D36 | **Reestructuración del menú principal (las 7 páginas reales).** (1) Se decide no incluir la sección de Blog en el menú visible de esta fase — **no se elimina la página**, `noticias-analisis.html` y los 2 artículos siguen existiendo y accesibles por URL directa, solo se quita el enlace del menú ("oculto"). (2) El enlace de Blog se reemplaza en el menú por **"Trabaja con nosotros"**, que apunta a la sección de postulación ya existente dentro de Nuestro Equipo (`nuestro-equipo.html#postulacion-heading`) — no se construyó página nueva, confirmado explícitamente por el Owner tras presentarle la alternativa (página independiente, como la antigua Talento AAA). (3) Las etiquetas del menú se renombran sobre las mismas páginas existentes, sin crear páginas nuevas (confirmado explícitamente): "Servicios" → **"Metodología"**, "Nuestro Equipo" → **"Quiénes somos"**. (4) "Nuestra Experiencia" se quita del menú (seguía sin construirse, apunta a `proximamente.html`). (5) "Contacto" se quita de la lista de enlaces regulares — el Owner inicialmente pidió "Mi caso" en su lugar (ya estaba así desde D34), pero corrigió: se mantiene el mismo botón destacado del header, solo que su texto vuelve a **"Contacto"** (revierte esa parte de D34, confirmado explícitamente con una pregunta de desambiguación). Menú final (`<ul>` de navegación regular): Inicio, Metodología, Quiénes somos, Trabaja con nosotros — más el botón destacado "Contacto" fuera de esa lista. En Nuestro Equipo, `aria-current="page"` queda solo en "Quiénes somos" — "Trabaja con nosotros" no lo lleva aunque apunte a la misma página, por ser un ancla a una sección, no la identidad de la página. **No se tocó** el segundo listado de navegación que existe en el **footer** ("Mapa del sitio") de las 7 páginas — el pedido del Owner fue explícitamente sobre "el main menú"; el footer conserva los 6 enlaces originales (Servicios/Nuestro Equipo/Blog/Contacto con sus nombres previos). Verificado visualmente en las 7 páginas (desktop y mobile) y probado el enlace de ancla de "Trabaja con nosotros" navegando directo a la URL — aterriza correctamente en el formulario de postulación, sin errores de consola. | Owner, 2026-08-20 | Parcialmente reemplazado por D42 — el texto del botón destacado ("Contacto") vuelve a "Mi caso"; el resto (menú regular, Blog oculto, footer sin cambios) sigue vigente |
| D37 | **Home: se quitan la franja "Logos clientes" y la banda de "pills" del equipo; el texto de "Dimensión humana" pasa antes que la foto.** El Owner señaló con capturas de pantalla tres bloques del prototipo que ya no son necesarios: (1) la franja de logos de clientes en placeholder (círculos "Logo 01"–"Logo 06") dentro de Casos de éxito; (2) la banda animada de "pills" con iniciales/nombre/cargo del equipo (Julián Agudelo, Angélica Chamorro, Jaime Marmolejo) dentro de Dimensión humana — esta banda se había agregado explícitamente a pedido del Owner el 2026-08-04; se retira ahora también a pedido explícito, no es una reversión no solicitada. (3) Además, en esa misma sección, el bloque de texto ("Detrás de cada proceso hay un equipo especializado...") pasa a ir **antes** que la fotografía (antes iba después) — cambio de orden en el DOM, no solo visual, para que el orden de lectura/foco coincida en cualquier viewport. Limpieza de código: se eliminó por completo el CSS de `.client-logo-*` y `.team-marquee`/`.team-pill` (sin uso en ningún HTML tras esta decisión); `.people-card` (el contenedor redondeado que envuelve texto+foto) se conserva sin cambios. | Owner, 2026-08-20 | Vigente — aplicado en `prototype/index.html` |
| D38 | **Rediseño de "Sectores de experiencia": cada sector muestra su ícono/nombre y los logos de sus clientes en la misma caja.** El Owner pidió este rediseño adjuntando un ejemplo de referencia (tarjeta "Financiero" con logos de BBVA y Banco de Occidente). **Antes de tocar código** se le señaló que ese ejemplo usa nombres de bancos reales y que el proyecto no tiene ningún cliente real aprobado todavía para asociar a un sector (mismo motivo por el que Casos de éxito y la franja retirada en D37 eran placeholders) — el Owner confirmó explícitamente usar **placeholders genéricos** ("Logo 01"/"Logo 02" por caja, mismo patrón ya establecido en el sitio), no nombres reales. Se reemplazan las dos filas de "marquee" con scroll continuo (`.sector-marquee`/`.sector-tile`, eliminadas por completo, sin uso restante) por una **grilla estática de 6 tarjetas** (mismo patrón responsive 3→2→1 columnas que `.focus-grid`) — decisión de diseño propia, no pedida explícitamente pero necesaria: con tarjetas más densas (ícono+nombre+logos) un marquee en movimiento habría sido más difícil de leer que el de solo ícono+nombre de antes. **Bug encontrado y corregido durante la verificación:** el comentario explicativo agregado junto con esta decisión contenía literalmente la secuencia `*/` en medio del texto (".sector-marquee\*/.sector-tile\*"), que en CSS cierra un comentario — esto cortaba el comentario a mitad de frase e invalidaba silenciosamente la regla `.sector-grid` completa (quedaba con `display:block` en vez de `display:grid`, sin ningún error visible). Detectado al verificar visualmente que la grilla no se veía en columnas; diagnosticado comparando `getComputedStyle` con el CSS fuente hasta aislar la secuencia `*/` accidental. Corregido reescribiendo el comentario sin esa secuencia; verificado que no hay otras instancias del mismo problema en todo el archivo (conteo de `/*` y `*/` balanceado, sin coincidencias de `*/` pegado a texto). | Owner, 2026-08-20 | Vigente — aplicado en `prototype/index.html` |
| D39 | **Se corrige el desborde de "Sectores de experiencia" hasta el margen total de la página.** El Owner señaló con captura que, a diferencia del resto del sitio, la grilla de 6 tarjetas (D38) se extendía hasta el borde de la ventana en vez de respetar el margen de contenido. Investigado antes de tocar código: `.sector-grid` era hijo directo de `<section>` (sin envolver en `.container`, a diferencia de `.sector-experience__header` en el mismo bloque), mientras que las grillas equivalentes que sí respetan el margen (`.situations-grid` en Situaciones, `.case-video-grid` en Casos de éxito) sí están dentro de `.container`. Se envuelve `.sector-grid` en `.container`, confirmando el diagnóstico (verificado con `getBoundingClientRect`: la grilla pasó de tocar los bordes de la ventana a alinearse con el mismo margen del encabezado de la sección). El Owner pidió además "el mismo efecto de slide que ya teníamos aquí": se identificó que el patrón existente es `.mobile-card-carousel` (usado en Situaciones y Casos de éxito, solo mobile ≤640px, scroll horizontal con snap) — se agrega esa misma clase a `.sector-grid` para que en mobile el comportamiento sea idéntico al resto del sitio, en vez de apilarse en una columna. Se retira `.sector-experience { overflow: hidden; }`, resto residual del antiguo marquee (D38) sin ningún uso ya que el contenido ahora está acotado por `.container`. | Owner, 2026-08-20 | Vigente — aplicado en `prototype/index.html` y `prototype/css/components.css` |
| D40 | Home, sección "Dimensión humana": se agrega un pequeño espacio entre el enlace "Conozca nuestra experiencia →" y la fotografía/placeholder debajo. La sección usa `.home-page .people-intro { gap: 0; }` (columna única, sin gap de grid) y ya tenía `padding-top` en `.people-intro__copy` para el espacio superior — se agrega `padding-bottom: var(--space-6)` al mismo selector (solo en el breakpoint desktop; en mobile ya existe `gap: var(--space-8)` real entre los bloques, así que no se duplica ahí). Verificado que `.people-intro`/`.people-intro__copy` no se usa en ninguna otra página del prototipo, así que el cambio no afecta nada fuera de Home. | Owner, 2026-08-20 | Vigente — aplicado en `prototype/css/components.css` |
| D41 | **Nueva sección "En cifras": contador animado bajo Sectores de experiencia.** El Owner pidió un contador con casos gestionados, número de clientes y cartera recuperada, con efecto de conteo ascendente (0 → N), adjuntando como referencia visual/de animación el sitio de otra empresa ("Meléndez") — se aclaró que solo se toma el efecto de animación, no sus cifras (son de otra empresa, sin relación con AAA). AAA no tiene todavía cifras propias aprobadas para estos tres indicadores — mismo caso que Casos de éxito y los logos de Sectores — así que se preguntó explícitamente al Owner (AskUserQuestion) si ya había cifras reales o si se construía con placeholders; confirmó placeholders "por ahora". Se implementó la sección completa y funcional: JS nuevo en `js/main.js` (`data-count-to`/`data-count-prefix`/`data-count-suffix`, disparado por IntersectionObserver, con `requestAnimationFrame` y ease-out cúbico; con `prefers-reduced-motion` salta directo al valor final sin animar, mismo criterio que el resto de animaciones del sitio) y CSS nuevo (`.stats-grid`/`.stat-card`, 3→1 columnas). Los tres valores mostrados (100 / 50 / $100 M) son **cifras de ejemplo, redondas a propósito para no parecer datos reales** — no se inventan cifras de AAA — y cada tarjeta incluye una leyenda visible "Cifra de ejemplo — pendiente de dato aprobado por el cliente" (mismo criterio que "Logo 01"/"Logo 02" en D38 e IMG-xx pendientes en el resto del sitio). Cuando el Owner entregue las cifras reales, basta reemplazar `data-count-to` y el texto de la leyenda; el mecanismo de conteo funciona igual sobre el dato real. | Owner, 2026-08-20 | Vigente — aplicado en `prototype/index.html`, `prototype/css/components.css` y `prototype/js/main.js`; cifras mostradas son placeholder, pendientes de reemplazo por dato real aprobado |
| D42 | **El texto del botón destacado del header vuelve de "Contacto" a "Mi caso" (las 7 páginas reales).** El Owner confirmó que la URL a la que apunta ese botón corresponde al flujo de la plataforma ADY (intake de casos), de la cual todavía no se comparte el enlace real — el botón no es un "Contacto" genérico, es la entrada al flujo "Mi caso"/ADY, así que el texto debe reflejar eso. Solo cambia el `<span class="btn--header-label">` del botón destacado del header en las 7 páginas (`index.html` + 6 páginas interiores); no se tocó el destino (`captacion-b2b.html` en Home, `#nota-ady` en las 6 páginas interiores — ancla ya documentada como pendiente de URL real de ADY en `prototype/README.md`, sin cambios, no era parte de este pedido) ni el enlace "Contacto" del **footer** (`.footer-links`, mismo criterio que D36: es un enlace de texto de una lista, no "el botón"). | Owner, 2026-08-20 | Vigente — aplicado en las 7 páginas reales del prototipo (botón destacado del header) |
| D43 | **Hero de Home: se quita el botón secundario "Conozca cómo lo hacemos" y el botón primario pasa de navegar a la página de contacto a hacer scroll a "Cómo intervenimos".** Ambigüedad resuelta con AskUserQuestion antes de tocar código: el Owner pidió "quitar el botón azul" del hero — el hero tenía dos botones, el primario naranja "Hablemos de su caso →" (a `captacion-b2b.html`) y el secundario de borde claro "Conozca cómo lo hacemos" (a `servicios.html`), y el pedido también decía "el texto de dicho botón lo reemplazamos por 'hablemos de su caso'" y "crea un ancla... a la sección Cómo intervenimos" — instrucción internamente ambigua entre repurposar el botón secundario (quedando dos botones con texto igual) o dejar un solo botón. El Owner confirmó la segunda opción: se elimina el botón secundario por completo; el botón primario ("Hablemos de su caso", sin cambio de texto porque ya decía eso) cambia su `href` de `captacion-b2b.html` a `#servicios-home-heading` (ancla al `<h2>` de "Cómo intervenimos", con scroll suave nativo ya existente en el sitio, `html { scroll-behavior: smooth }`). El hero **pierde su enlace directo a la página de contacto** — señalado explícitamente al Owner, es una consecuencia intencional de la instrucción, no un descuido. Como el header de Home es `position: fixed` (78px de alto en desktop, 68px en mobile ≤640px), se agregó `scroll-margin-top` al `<h2 id="servicios-home-heading">` (con margen holgado para que también quede visible el eyebrow "Cómo intervenimos" encima del título, no solo el título) — verificado con `getBoundingClientRect()` en una pestaña nueva (sin caché) tanto en desktop como en mobile, confirmando que ni el eyebrow ni el título quedan tapados por el header tras el salto. | Owner, 2026-08-20 | Vigente — aplicado en `prototype/index.html` y `prototype/css/components.css`; el hero de Home ya no enlaza directo a `captacion-b2b.html` |
| D44 | **Hero de Home: el texto del botón único cambia de "Hablemos de su caso" a "Conozca cómo lo hacemos"; se reduce el espacio entre el título y las cards en Situaciones.** (1) Botón del hero: solo cambia el texto (reutiliza el texto del botón retirado en D43) — el `href` sigue siendo la ancla a `#servicios-home-heading` fijada en D43, sin cambios. (2) El Owner señaló con una captura que, en la sección "¿Es acreedor y se enfrenta a alguno de estos riesgos?" (Situaciones), el espacio entre el título y las 3 cards es demasiado grande. Investigado antes de tocar código: ese espacio viene de `.home-page .section-head { margin-bottom: var(--space-16) }` (64px) — una regla **compartida por todas las secciones de Home** con `.section-head` (Situaciones, Cómo intervenimos, Por qué AAA, En cifras, Casos de éxito), no específica de Situaciones. Se agrega un override **scoped solo a esta sección** (`section[aria-labelledby="situaciones-heading"] .section-head { margin-bottom: var(--space-8) }`, 32px — la mitad) en vez de tocar la regla compartida, para no afectar el espaciado de las demás secciones — confirmado con `getComputedStyle` que el resto de `.section-head` de Home siguen en 64px sin cambios. | Owner, 2026-08-20 | Vigente — aplicado en `prototype/index.html` y `prototype/css/components.css` |
| D45 | **Los títulos de las tarjetas en "Sectores de experiencia" y "¿Por qué AAA?" pasan de la serif de display (Libre Caslon Display) a Krub bold.** El Owner señaló con dos capturas anotadas los nombres de sector (Financiero, Fintech, Solidario, Sector público, Salud, Cajas de compensación) y los 5 títulos de "¿Por qué AAA?" (Representación exclusiva del acreedor, Tecnología propia (ADY), Cobertura nacional, Equipo especializado, Seguridad de la información). Se cambia `font-family` de `var(--font-display)` a `var(--font-sans)` (Krub) y `font-weight` de 400 a 700 en `.sector-card__name` y en `.home-page .focus-grid h3` — Krub 700 ya estaba cargado vía Google Fonts en `index.html` (`Krub:wght@400;500;600;700`), no hizo falta agregar ningún peso nuevo. Cambio acotado a esos dos selectores específicos — no se tocó la fuente de display en ningún otro título del sitio. | Owner, 2026-08-20 | Vigente — aplicado en `prototype/css/components.css` |
| D46 | **"¿Por qué AAA?": se reduce el tamaño de los títulos de tarjeta (1.5rem → 1.3rem) porque el texto quedaba casi pegado al borde inferior de la card.** El Owner señaló con una captura anotada que, tras D45 (títulos a Krub bold), el texto de las tarjetas llegaba al borde de la caja, y propuso agregar padding o reducir un poco el tamaño de fuente. Investigado antes de tocar código: la causa no era falta de padding (`.home-page .focus-grid > .surface-solid` ya tenía `padding: var(--space-8)` = 32px sin cambios) sino que Krub bold es más ancha por carácter que la serif que reemplazó — al mismo tamaño y con el mismo `max-width: 13ch` del título, un texto como "Representación exclusiva del acreedor." pasó de envolver en 3 líneas a 4, y esa altura extra no cabía cómoda en la card de alto fijo (`grid-auto-rows: 220px`), quedando el texto muy cerca del borde. Se opta por la segunda alternativa que ofreció el Owner (reducir la fuente) porque ataca la causa real (menos líneas/menor alto de bloque de texto) en vez de solo compensarla con más padding, que habría reducido aún más el espacio disponible. Se bajó `font-size` de 1.5rem a 1.3rem en `.home-page .focus-grid h3` y en el selector duplicado `.home-page .focus-grid > :first-child h3` (mismo valor, para la primera tarjeta). Verificado con `getBoundingClientRect()`: el título de la primera tarjeta ahora vuelve a 3 líneas, con ~49px de aire entre el final del texto y el borde inferior de la card (antes, tocaba/rozaba el borde). No se tocó el breakpoint mobile (≤560px), que ya tenía su propio `font-size: 1.25rem` independiente de este ajuste. | Owner, 2026-08-20 | Vigente — aplicado en `prototype/css/components.css` |
| D47 | **Home § "Cómo intervenimos": la descripción de cada bloque (`.focus-mosaic`) solo aparece con hover, complementando el efecto existente.** El Owner señaló que el módulo (efecto hover + diseño) le gustaba, y pidió que las 3 descripciones de párrafo solo se muestren al pasar el mouse sobre su bloque, ocultándose al retirarlo. Aplicado **solo** dentro del mismo media query que ya usa el resto del efecto hover de este componente (`(hover: hover) and (pointer: fine) and (min-width: 1024px)`): en touch/mobile/tablet no existe un gesto de "hover" real, así que ahí la descripción se queda siempre visible (si se hubiera ocultado también en touch, habría quedado inaccesible sin alternativa de revelarla). Dentro de ese media query, `.surface-visual__caption p` arranca en `opacity: 0` con una transición sutil (`opacity` + `translateY(6px)`, 400ms, mismo timing `--motion-ease` del resto del componente) y pasa a visible en `:hover` y `:focus-within` del bloque — se agregó `:focus-within` además de `:hover` siguiendo el mismo patrón ya usado en `.surface-visual` genérico del sitio, aunque hoy no hay ningún elemento enfocable dentro de estos bloques. `prefers-reduced-motion: reduce` fuerza la descripción a quedar siempre visible y sin transición (mismo criterio que el resto de animaciones del sitio: sin movimiento, el contenido no depende de un gesto). Verificado con `getComputedStyle` (opacidad 0 en desktop antes de hover, 1 tras hover simulado, 1 siempre en mobile 375px) y visualmente con hover real sobre cada uno de los 3 bloques. | Owner, 2026-08-20 | Vigente — aplicado en `prototype/css/components.css` |
| D48 | **Servicios § "Nuestra metodología": rediseño a flujograma horizontal de 3 nodos conectados, para que quepa en una sola pantalla sin scroll.** El cliente (vía el Owner) pidió que Diagnóstico, Estrategia y Representación se vean completos en el mismo módulo sin necesidad de hacer scroll, "idealmente" con aspecto de flujograma. **Nota de trazabilidad importante:** en esta misma fecha, más temprano en el proyecto, ya se había probado un flujograma de nodos conectados para esta sección (D32) y el Owner pidió revertirlo (D33) — antes de tocar código se señaló explícitamente esta historia y se confirmó con el Owner (AskUserQuestion) que este es un pedido nuevo e intencional del cliente, no una repetición de la idea ya rechazada; confirmó que sí. También se confirmó (segunda pregunta) cómo mostrar las 3 descripciones sin acortar el copy real: aparecen solo con hover/foco del nodo — mismo patrón recién aprobado en D47 (Home § "Cómo intervenimos"), en vez de reescribir los 3 párrafos a versiones resumidas. **Aplicado:** nuevo modificador `.stepper--flowchart` (reemplaza `.stepper--compact` de D33 en esta sección), scoped a `servicios.html` — Captación B2B § "Nuestro proceso" usa el mismo componente base (`.stepper__grid`/`.stepper__intro`/`.stepper__steps`/`.stepper__step`) sin este modificador y no se ve afectada (verificado explícitamente: sigue con `flex-direction: column` y `min-height: 450px`, su comportamiento original). Se agregaron 2 flechas conectoras (`.stepper__connector`, SVG decorativo `aria-hidden`) entre los 3 pasos en el HTML. Desde 801px: fila horizontal con nodos compactos (ícono en círculo + título, sin párrafo visible) unidos por las flechas; la descripción completa aparece como popover (`position: absolute`, no afecta el layout del resto de la página) al hacer `:hover`/`:focus-within` sobre el nodo, con la misma transición de opacidad que D47. Ese ocultamiento queda acotado al mismo guard de hover real (`(min-width: 801px) and (hover: hover) and (pointer: fine)`): en touch/tablet ≥801px sin hover real, la descripción vuelve a mostrarse siempre (estática, no popover); en mobile (≤800px) se degrada a lista vertical simple con descripción siempre visible, igual criterio que D47. `prefers-reduced-motion: reduce` también deja la descripción sin transición. | Owner, 2026-08-20 | Vigente — aplicado en `prototype/servicios.html` y `prototype/css/components.css`; reemplaza D33 (`.stepper--compact`) solo en esta sección |
| D49 | **Flujograma de "Nuestra metodología" (D48): en mobile/tablet (≤800px) el ícono pasa a estar al lado izquierdo del título, en vez de encima, para ahorrar espacio vertical.** El Owner señaló con una captura que, en la vista apilada de mobile/tablet, el ícono ocupaba su propia línea completa sobre el título, gastando espacio innecesario — pidió el ícono a la izquierda del texto del título, solo en esa vista (no en desktop, donde el diseño de nodo circular centrado ya está aprobado). Se agrega un wrapper nuevo `<div class="stepper__step-head">` alrededor del ícono y el `<h3>` de cada uno de los 3 pasos, solo en el HTML de `servicios.html` (no toca el componente base compartido con Captación B2B, que no tiene este wrapper). En la regla base de `.stepper--flowchart` (mobile/tablet, sin media query), `.stepper__step-head` es `display:flex; align-items:center; gap` — ícono y título en fila. Desde 801px se revierte a `flex-direction:column` (mismo diseño de nodo circular ya vigente de D48, sin cambios visuales en desktop). | Owner, 2026-08-20 | Vigente — aplicado en `prototype/servicios.html` y `prototype/css/components.css`, solo en la vista ≤800px del flujograma |
| D50 | **Flujograma de "Nuestra metodología": en mobile/tablet, la línea divisoria horizontal entre pasos se reemplaza por la misma flecha del conector de desktop, rotada 90° (apunta hacia abajo).** El Owner señaló con una captura que, en la vista apilada de mobile/tablet, la línea divisoria heredada del componente base (`.stepper__step { border-top: ... }`) rompía la sensación de "flujograma" — preguntó si se podían quitar esas líneas y usar líneas verticales o las mismas flechas de la versión desktop. Se optó por reutilizar las flechas existentes (`.stepper__connector`, ya en el HTML entre los 3 pasos desde D48, ocultas en mobile hasta ahora) en vez de agregar líneas nuevas — mismo elemento visual en las 3 vistas, más consistente. `.stepper--flowchart .stepper__step` pierde su `border-top` (override con mayor especificidad que la regla compartida, sin tocarla); `.stepper__connector` pasa de `display:none` a visible, centrado, con el mismo ícono de flecha SVG que ya se usa en desktop pero con `transform: rotate(90deg)` (la flecha horizontal → pasa a apuntar hacia abajo ↓). El bloque `@media (min-width: 801px)` que ya sobrescribe `.stepper__connector` para la fila horizontal agrega `transform: none` sobre el SVG, para que la rotación de mobile no se herede en desktop. | Owner, 2026-08-20 | Vigente — aplicado en `prototype/css/components.css`, solo en la vista ≤800px del flujograma |
| D51 | **Home § "¿Por qué AAA?": reorden completo y ampliación de 5 a 6 atributos.** El Owner pidió este orden izquierda→derecha: Infraestructura para atención masiva (pidiendo revisar/acortar el texto), Equipo especializado, Tecnología (ADY), Seguridad de la información, Compliance, Cobertura nacional. Se retira "Representación exclusiva del acreedor" (no estaba en la lista nueva) y se agregan 2 atributos nuevos: "Infraestructura para atención masiva" y "Compliance". Para el texto del primer atributo se presentaron 3 opciones cortas con AskUserQuestion (mismo tono que el resto: sustantivo + punto); el Owner eligió **"Infraestructura para atención masiva."** Para el ícono de "Compliance" se reutiliza el ícono de escudo+check que antes tenía "Representación exclusiva del acreedor" (el concepto de verificación/certificación encaja igual de bien, evita crear un ícono nuevo innecesario); "Infraestructura para atención masiva" recibe el único ícono nuevo (rack de servidores, 3 barras con indicador). El resto de textos e íconos existentes (Equipo, Tecnología/ADY, Seguridad, Cobertura) se reutilizan sin cambios, solo reordenados. **Rediseño de la grilla:** con 6 tarjetas (número par) ya no hacía falta el mosaico asimétrico de spans/offsets que exigía el conteo impar de 5 (D24) — se simplifica a una grilla regular 6 → 3 → 2 columnas (sin media query especial por posición). El color alternado (blanco / navy-deep / azul claro) que antes estaba hardcodeado a las posiciones 2/3/5 de la lista de 5 pasa a un ciclo de 3 vía `:nth-child(3n+2)`/`:nth-child(3n)`, para que el ritmo visual se mantenga automáticamente aunque se reordenen tarjetas en el futuro. Verificado con JavaScript (el Browser pane no pudo tomar captura en este turno): orden y textos correctos, colores en el ciclo esperado (blanco/navy/azul/blanco/navy/azul), grilla en 6 columnas ≥1200px, 3 columnas 801-1199px, 2 columnas ≤800px, carrusel horizontal ≤640px con las 6 tarjetas del mismo ancho, íconos nuevos renderizando con las dimensiones esperadas, sin errores de consola. | Owner, 2026-08-21 | Parcialmente reemplazado por D52 — la grilla ya no usa 6 columnas en una fila en desktop; el resto (orden, textos, íconos, color alternado en ciclo de 3) sigue vigente |
| D52 | **Home § "¿Por qué AAA?": en desktop, la grilla pasa de 6 columnas en una sola fila a 3+3 (dos filas de 3).** El Owner señaló con una captura que, en una sola fila de 6, el título más largo ("Infraestructura para atención masiva.") no cabía dentro de la card sin desbordarse — y aclaró explícitamente que bajar más el tamaño de fuente no era una opción (ya se había reducido una vez en D46 para otro problema de espacio). El breakpoint intermedio de D51 (3 columnas entre 801-1199px) ya existía y se veía bien — se simplifica el CSS haciendo que esas mismas 3 columnas sean el layout **por defecto** en desktop también, en vez de solo activarse por debajo de 1199px; se elimina el breakpoint `@media (max-width: 1199px)` que quedó redundante. Sin cambios en 2 columnas (≤800px) ni en el carrusel móvil (≤640px). Verificado visualmente (captura) en 1280px: 2 filas de 3 tarjetas, el título de "Infraestructura para atención masiva." se lee completo sin desbordarse; confirmado con `getComputedStyle` que a 700px sigue en 2 columnas, sin cambios. Sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/css/components.css` |
| D53 | **Home § "¿Por qué AAA?": se reduce el espacio en blanco de cada tarjeta (ícono y título ya no quedan estirados a los extremos de la card) y se baja el alto de card en las 4 vistas responsive.** El Owner señaló con una captura anotada el vacío grande entre el ícono y el título, y entre el título y el borde inferior — pidió reducirlo para que el módulo no llenara el viewport con elementos tan grandes. Causa: estas tarjetas solo tienen ícono + título (sin párrafo), pero el contenedor usaba `justify-content: space-between` dentro de una card de alto fijo (220px), lo que estiraba el ícono al extremo superior y el título al extremo inferior, dejando un vacío en el medio. Se cambia a `justify-content: flex-start` con `gap: var(--space-4)` entre ícono y título (agrupados arriba, separación chica), se quita el `margin-bottom` sobrante del `<h3>` (ya no hacía falta, era el último elemento) y se reduce el padding de la card (`--space-8` → `--space-6`). En conjunto con esto, se reduce el alto de card en las 4 vistas responsive: 220px → 160px en desktop (3+3), tablet (2 columnas ≤800px y ≤560px) y el carrusel móvil (≤640px, antes 210px). Se limpió además un conflicto de especificidad detectado durante la verificación: una regla combinada (`.home-page .focus-grid > .surface-solid, .home-page .grid-2--stretch > .surface-solid { min-height: 210px }`, declarada más abajo en el archivo) habría sobrescrito silenciosamente el nuevo alto de 160px del carrusel móvil por empatar en especificidad y venir después en el código — se quita `.focus-grid` de esa regla combinada, dejándola solo para `.grid-2--stretch` (Situaciones), que no fue parte de este pedido y no cambia. Verificado visualmente en 1280px (3+3), 700px (2 columnas) y 375px (carrusel móvil): ícono y título agrupados arriba en las 3 vistas, sin vacío perceptible, cards visiblemente más compactas. Sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/css/components.css` |
| D54 | **Sectores de experiencia: el ícono + nombre de cada tarjeta pasa a estar centrado (antes alineado a la izquierda).** Pedido directo del Owner sobre la captura de las 6 tarjetas de sector. Se agrega `justify-content: center` a `.sector-card__header` (el contenedor flex que agrupa ícono+nombre) — el divisor horizontal debajo (border-top de `.sector-card__logos`) sigue ocupando el ancho completo de la card sin cambios, así que el título queda centrado sobre una línea que sigue de borde a borde. Verificado visualmente en desktop (1280px); sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/css/components.css` |
| D55 | **Header full-width de Home replicado idéntico en las 7 páginas reales, reemplazando la píldora flotante de las 6 páginas interiores.** El Owner pidió replicar exactamente el header de Home en todas las páginas y convertirlo en un "elemento global" para que un cambio futuro se refleje en todas. **Antes de tocar código**, se señaló con AskUserQuestion que esto revierte explícitamente la parte de D34 donde el Owner había decidido NO replicar la arquitectura completa de Home (mantener la píldora flotante en las páginas interiores) — confirmó que es un pedido nuevo e intencional. Segunda pregunta: como este prototipo es HTML estático puro (sin sistema de plantillas ni build), verdadera reutilización "un cambio, se aplica en todas" no es técnicamente posible todavía — se le presentaron dos caminos (duplicar el HTML/CSS ahora, con reutilización real solo cuando exista el template part de WordPress; o agregar un include vía JavaScript ya en el prototipo, con el costo de que el menú apareciera un instante después de cargar la página y de que el sitio pasara a depender de JS para la navegación, algo que hasta ahora evita por diseño — D7/D18). El Owner eligió duplicar el HTML/CSS ahora. **Aplicado:** nuevo modificador `.site-header--global`, puesto directamente en el `<header>` (no en `<body>`) para no arrastrar el resto de los overrides de `.home-page` (hero, secciones, stepper, focus-grid, etc., que siguen siendo exclusivos de Home) — se migran ~30 selectores de `.home-page .site-header`/`.brand`/`.nav`/`.btn--header` (y sus 3 breakpoints) a `.site-header--global`, mismas reglas y valores, sin cambios visuales en Home. Se replica el HTML exacto del header de Home (logo real con isotipo+wordmark claro/oscuro, anotaciones `wp-block-*`, menú de 4 ítems) en las 6 páginas interiores, conservando el `href` del CTA que cada página ya tenía (`#nota-ady`, pendiente de la URL real de ADY — D42) y el `aria-current="page"` correspondiente a cada una. `js/main.js`: el breakpoint de colapso del menú (antes 1180px solo en Home, 1023px en el resto) pasa a ser 1180px en las 7 páginas, ya que ahora comparten el mismo header de 3 zonas. **No se tocó** `proximamente.html` (stub minimalista fuera del alcance de este C2, mismo criterio que D34) ni `type-lab.html` (página de referencia visual, no es parte del sitio real). Verificado visualmente en las 7 páginas (desktop 1280px, colapso de menú a 1000px, mobile 375px) y con `grep` que los conteos de `<div>`/`</div>` quedan balanceados en cada archivo tras la edición. Sin errores de consola en ninguna página. | Owner, 2026-08-21 | Parcialmente reemplazado por D34/D36 — la parte de esas decisiones sobre mantener la píldora flotante en las páginas interiores queda revertida; lo demás (menú de 4 ítems, CTA "Mi caso", etc.) sigue vigente. Vigente — aplicado en las 7 páginas reales, `prototype/css/components.css` y `prototype/js/main.js` |
| D56 | **Sectores de experiencia: cada chip de logo pasa a llevar una marca abstracta genérica junto al texto "Logo 01"/"Logo 02", para ilustrar al cliente cómo se vería el espacio con logos reales.** El Owner pidió agregar "logos de ejemplo" en los chips, y también preguntó (pregunta exploratoria, no un pedido de implementación) si convendría un efecto de slide para cuando los logos no quepan en el contenedor. Respuesta dada sobre el slide: se recomendó NO implementarlo por ahora — un carrusel horizontal dentro de una card pequeña es fácil de pasar por alto y poco natural con mouse; el patrón estándar para una pared de logos es dejarlos hacer wrap (que `.sector-card__logos` ya hace, `flex-wrap: wrap`) o, si un sector llega a tener muchos logos reales, un tope con "+N más" — el `.mobile-card-carousel` que ya existe en el sitio queda como opción a evaluar más adelante si el wrap no alcanza, una vez haya cifras reales de cuántos logos por sector. Sobre los logos de ejemplo: como no hay clientes reales aprobados por sector (mismo criterio que D38), se preguntó con AskUserQuestion qué tratamiento visual usar en vez de inventar marcas reales — el Owner eligió una marca abstracta genérica (sin ninguna marca real) + texto. Se agregan 2 íconos SVG abstractos distintos (uno para la posición "Logo 01", un hexágono; otro para "Logo 02", un mark tipo "órbita" de dos círculos) — misma forma reutilizada en las 12 ocurrencias (6 sectores × 2 logos), para que no se vean todas idénticas sin necesitar 12 formas distintas. Verificado visualmente en desktop (con zoom de página para inspeccionar el detalle a este tamaño) y mobile (carrusel horizontal); sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/index.html` y `prototype/css/components.css` |
| D57 | **El header (logo/menú) queda siempre alineado con el texto del hero/título de cada página, en cualquier ancho de pantalla.** El Owner señaló con una captura que en Home el logo del header y el texto del hero comparten el mismo margen izquierdo, y pidió normalizar ese mismo padding en las demás páginas. Investigado antes de tocar código: el header usaba una escala de padding propia (`clamp(1.5rem, 4vw, 4rem)`, más generosa) en vez de `--container-padding` (el token que ya usan `.container` y el hero de Home, `clamp(1rem, 4vw, 2rem)`) — se corrige para que el header use `var(--container-padding)` directamente. Además, el header tenía `max-width: 1440px` mientras el resto del sitio usa `--container-max` (1200px) — con paddings iguales pero anchos máximos distintos, header y contenido se centraban en puntos diferentes en viewports entre ~1200-1440px; se corrige a `max-width: var(--container-max)`. Con ambos cambios, Home queda perfectamente alineado en cualquier ancho (verificado en 1280/1440/1850/1920px). **Hallazgo adicional, consultado con el Owner (AskUserQuestion) antes de tocarlo:** Servicios y Contacto usan un hero de foto a pantalla completa (`.hero-shell`, sin límite de ancho a propósito — REF-BIOFARMA, decisión de 2026-07-31) que en monitores anchos (~1920px) dejaba el texto del hero hasta ~350px más a la izquierda que el header, porque esa foto no respeta `--container-max` como el resto del sitio. Se le presentó la disyuntiva (alinear el texto aceptando que la foto deje de llegar a los bordes en pantallas muy anchas, o dejarlo como estaba) — el Owner eligió priorizar la alineación del texto. Se agrega `max-width: var(--container-max)` + `margin-inline: auto` a `.hero-shell` (antes sin límite), con una restauración explícita en `.home-page .hero-shell` (Home no debía heredar este tope — su alineación ya funcionaba por su propio mecanismo independiente, `.home-page .hero-shell__content`). El contenido del hero (`.hero-shell__content`) pasa del mismo patrón que ya usa Home (`width: min(100%, var(--container-max))` + `margin-inline: auto` + `padding: ... var(--container-padding) ...`), en vez de un cálculo de compensación (`calc(container-padding - hero-inset)`) que solo funcionaba correctamente en el modo "foto casi de borde a borde" y no en el modo "foto ya topada a su ancho máximo" — con el patrón de Home, ambos modos coinciden exactamente. El límite de ancho para el texto (antes `max-width: 64ch` en la caja completa) se traslada al párrafo específicamente (`.hero-shell__content > p`), ya que el título tiene su propio límite (`max-width: 23ch`) y la caja contenedora ahora necesita poder crecer hasta `--container-max` para el mecanismo de autocentrado. Verificado con `getBoundingClientRect()` que el logo del header y el título del hero quedan en el mismo píxel izquierdo en Home, Servicios y Contacto a 1280px, 1440px y 1920px de ancho — y que Home conserva su foto a pantalla completa sin cambios (ancho de `.hero-shell` casi igual al viewport, no capado). Las 4 páginas restantes (Quiénes somos, Blog y sus 2 artículos), que usan encabezados simples dentro de `.container` sin hero de foto, ya estaban alineadas de antes y no se tocaron. Verificado en mobile (375px) sin errores de consola en ninguna página. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/css/components.css` |
| D58 | **Sectores de experiencia: los 12 chips placeholder de D56 ("Logo 01"/"Logo 02" + marca abstracta) se reemplazan por los logos reales de los 15 clientes actuales de AAA, ahora que el Owner los aportó (archivos en `assets/img/`).** El Owner cargó localmente los logos y pidió reemplazar los dummies; antes de tocar código se verificó con AskUserQuestion a qué sector de los 6 existentes (Financiero, Fintech, Solidario, Sector público, Salud, Cajas de compensación) pertenece cada cliente real, ya que asignar mal un cliente real a un sector es un error de dato, no un problema cosmético (mismo criterio "no inventar" de D38/D56). El Owner confirmó 8 mapeos claros por inspección visual (BBVA→Financiero, Comfandi→Cajas de compensación, Gobernación del Valle del Cauca→Sector público, MetroCali→Sector público, Coopunidos→Solidario, Fetrabuv→Solidario, Fonalianza→Solidario, Promédico→Salud) y aclaró el giro de 4 más ambiguos (Femcristar: fondo de empleados→Solidario; K-Listo: productos alimenticios; Neat: gestión inmobiliaria boutique; Nuestro Fondo: ahorro y crédito), delegando la ubicación final de estos y de otros 3 sin encaje sectorial claro (Constructora Meléndez, El Gran Langostino, Ma. Isabel) a nuestro criterio ("ubícalos donde te parezca más apropiado" / "van repartidos en los sectores existentes", no crear un 7º sector). Asignación final aplicada: Financiero (BBVA, El Gran Langostino), Fintech (Neat, Nuestro Fondo), Solidario (Coopunidos, Fetrabuv, Fonalianza, Femcristar), Sector público (Gobernación del Valle del Cauca, MetroCali, Constructora Meléndez), Salud (Promédico, Ma. Isabel), Cajas de compensación (Comfandi, K-Listo) — las últimas 6 asignaciones (Neat, Nuestro Fondo, Meléndez, El Gran Langostino, Ma. Isabel, K-Listo) son juicio propio delegado por el Owner, no un giro de negocio confirmado por el cliente, y quedan fácilmente ajustables si el Owner/cliente prefiere otra ubicación. **Trabajo técnico:** los 15 PNG (todos con fondo transparente, verificado con `System.Drawing` — alpha=0 en la esquina de cada archivo) se movieron de la subcarpeta local `assets/img/Logos - Clientes/` (con espacios, fuera de la convención kebab-case del proyecto) a la carpeta plana `assets/img/`, corrigiendo de paso el nombre `logo-construtora melendez.png` (typo + espacio) a `logo-constructora-melendez.png`; el archivo fuente `.ai` no se movió (no es un asset web). En `index.html` cada uno de los 12 chips `<span class="sector-card__logo">` pasa de contener un SVG abstracto + texto a un `<img>` real (`loading="lazy"`, `alt` con el nombre del cliente); Solidario ahora tiene 4 logos y Sector público 3, en vez de forzar 2 por card — se prioriza mostrar el conteo real de clientes por sector sobre la simetría visual, y `.sector-card__logos` ya soportaba wrap (`flex-wrap: wrap`) sin necesitar un carrusel/slide de overflow (mismo criterio ya sentado en D56). En `components.css`, `.sector-card__logo` deja de ser una caja con borde/fondo/texto (era el tratamiento del placeholder) y pasa a ser solo un contenedor de imagen a altura fija (40px); se agrega `.sector-card__logo img { max-height:100%; max-width:120px; object-fit:contain }`; se elimina `.sector-card__logo-mark` (la marca SVG abstracta de D56, sin más uso). Verificado localmente (servidor estático temporal en :4173): los 15 `<img>` cargan con 200 OK, sin errores de consola, en desktop (1280px, wrap correcto en Solidario/Sector público) y mobile (375px, carrusel horizontal). | Owner, 2026-08-21 | Vigente — aplicado en `prototype/index.html`, `prototype/css/components.css` y `prototype/assets/img/`; reemplaza D56 |
| D59 | **Sectores de experiencia: se quita la línea divisoria entre el ícono/nombre y los logos, y los logos suben de 40px a 64px de alto (máx. ancho 120px→160px) — se veían muy pequeños incluso en desktop.** El Owner señaló, sobre una captura del resultado de D58, que la distribución de logos por sector le gustó, y pidió dos ajustes puntuales: quitar la línea (`border-top`) que separaba el header de la fila de logos en `.sector-card__logos`, y agrandar los logos. Se quita el `border-top: 1px solid var(--color-border-flat)` (el `padding-top` que ya existía queda como único separador de aire entre header y logos, sin línea). Se sube `.sector-card__logo` de `height: 40px` a `64px` y `.sector-card__logo img` de `max-width: 120px` a `160px` — mismo criterio `object-fit: contain` de D58, sin distorsión. Verificado localmente (servidor estático en :4173): sin línea divisoria, logos visiblemente más grandes y legibles en desktop (1280px, wrap sin problemas en Solidario/Sector público con 4/3 logos) y mobile (carrusel horizontal, 375px); sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/css/components.css` |
| D60 | **Sectores de experiencia: los logos suben otro poco, de 64px a 76px de alto (máx. ancho 160px→190px) — ajuste fino a pedido del Owner tras ver D59 en vivo ("un tris más grandes").** Mismo mecanismo de D58/D59 (`.sector-card__logo` altura fija, `.sector-card__logo img` con `object-fit: contain`), solo se sube el valor. Verificado localmente (servidor estático en :4173): logos visiblemente más grandes sin romper el wrap de las cards con más de 2 logos (Solidario con 4, Sector público con 3 siguen en una sola fila a 1280px); sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/css/components.css` |
| D61 | **Sectores de experiencia: los logos bajan de 76px a 75px de alto — corrección de un valor de más pedida por el Owner tras D60.** Ajuste mínimo, mismo mecanismo. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/css/components.css` |
| D62 | **Sectores de experiencia: se crean 2 sectores nuevos ("Construcción e inmobiliario" y "Comercio y consumo") para los 5 logos que no encajaban bien en los 6 sectores originales — el sitio pasa de 6 a 8 sectores.** El Owner vio en vivo el resultado de D58 (donde Constructora Meléndez, Neat, El Gran Langostino, Ma. Isabel y K-Listo habían sido repartidos por criterio propio dentro de los 6 sectores existentes, a pedido explícito del Owner en ese momento) y señaló que esa distribución forzada "no quedó muy bien" — se notaba que esos 5 clientes no son bancos, fintech, fondos de empleados, entidades públicas, prestadores de salud ni cajas de compensación. Antes de tocar código, dado que esto cambia una afirmación de posicionamiento real del sitio (el párrafo de intro nombra explícitamente los sectores donde AAA tiene experiencia), se confirmó con AskUserQuestion cómo agrupar los 5 logos sin encaje: se presentaron 3 opciones (2 sectores nuevos por afinidad temática; 1 sector nuevo genérico con los 5 juntos; que el Owner especificara los nombres/grupos él mismo) — el Owner eligió **2 sectores nuevos por afinidad**. **Aplicado:** "Construcción e inmobiliario" (Constructora Meléndez — constructora — y Neat — gestión inmobiliaria boutique — ambos del rubro de bienes raíces/construcción) y "Comercio y consumo" (El Gran Langostino — pescados y mariscos —, Ma. Isabel y K-Listo — ambos productos alimenticios — los 3 son comercio/retail de consumo). Nuestro Fondo se queda en Fintech sin cambios (ahorro y crédito sí encaja ahí, no fue parte del problema señalado). Los 6 sectores originales quedan con 1-4 logos cada uno tras retirar los que se movieron (Financiero: solo BBVA; Fintech: solo Nuestro Fondo; Sector público: Gobernación + MetroCali; Salud: solo Promédico; Cajas de compensación: solo Comfandi; Solidario sin cambios, 4 logos). Se agregan 2 íconos SVG nuevos en el mismo estilo de línea que el resto (viewBox 256, stroke-width 14): una casa/edificio para Construcción e inmobiliario, una bolsa de compras para Comercio y consumo. El párrafo de intro de la sección (`sector-experience__intro`) se actualiza para nombrar los 8 sectores en vez de 6. Verificado localmente (servidor estático en :4173): los 8 bloques renderizan con sus logos e íconos correctos, sin errores de consola, en desktop (1280px, wrap de la grilla 3→3→2 sin problemas) y mobile (carrusel horizontal, 375px, 8 tarjetas deslizables). | Owner, 2026-08-21 | Vigente — aplicado en `prototype/index.html`; reemplaza parcialmente D58 (la distribución de esos 5 logos, no el resto) |
| D63 | **Femcristar pasa de Solidario a Financiero.** A diferencia de D58/D62 (donde el Owner delegó el criterio de ubicación), aquí dio la instrucción exacta y directa, sin necesidad de pregunta previa. Solidario queda con 3 logos (Coopunidos, Fetrabuv, Fonalianza); Financiero pasa a 2 (BBVA, Femcristar). Verificado localmente (servidor estático en :4173): Femcristar aparece en la tarjeta de Financiero junto a BBVA, ya no en Solidario; sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/index.html` |
| D64 | **"¿Por qué AAA?": las 6 tarjetas pasan de rectángulos anchos y bajos a un formato casi cuadrado, y la fila de abajo (Seguridad de la información, Compliance, Cobertura nacional) deja de repetir el mismo ciclo de color que la fila de arriba.** El Owner pidió dos cosas sobre una captura del módulo: (1) que las cards se vean "casi como si fueran cuadrados, pero con el espacio justo para que no dañe los títulos" — el alto de card (`grid-auto-rows`) sube de 160px a 260px en desktop (3 columnas), 240px en tablet (≤800px, 2 columnas) y 230px en el carrusel móvil (≤640px, antes 160px fijo en `.surface-solid`) — revierte parcialmente D53 (que había bajado el alto por sobra de espacio en blanco), pero por una razón distinta (forma, no espaciado): con el mismo contenido (ícono + título agrupados arriba, sin `space-between`, ya fijado en D53) el título más largo ("Infraestructura para atención masiva.") sigue con aire de sobra en la card ahora más alta, sin riesgo de desbordarse. (2) Que los 3 colores de la fila de abajo cambien para no verse "iguales" a los de arriba: Seguridad de la información = azul oscuro, Compliance = azul claro, Cobertura nacional = blanco — el orden queda invertido respecto a la fila de arriba (blanco/azul oscuro/azul claro). Esto rompe el supuesto de D51 de que un ciclo automático (`:nth-child(3n+2)`/`:nth-child(3n)`) bastaría para cualquier reorden futuro — se reemplaza por selectores explícitos por posición (`:nth-child(1)` a `:nth-child(6)`), ya que ahora cada fila tiene su propio patrón de color, no uno cíclico. Verificado localmente (servidor estático en :4173) en desktop (1280px, 3 columnas), tablet (700px, 2 columnas) y el carrusel móvil (375px, deslizado hasta el final): las 6 cards se ven notablemente más cuadradas, títulos con aire de sobra en las 3 vistas, colores de fila de abajo correctos (navy/azul claro/blanco) en las 3 vistas; sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/css/components.css`; reemplaza parcialmente D51 (la lógica de ciclo automático) y D53 (el alto de card, por una razón distinta) |
| D65 | **"¿Por qué AAA?": se revierte D64 — las cards vuelven a ser rectangulares (no cuadradas), con mucho menos espacio vacío abajo y a la derecha del título.** El Owner vio D64 en vivo y pidió lo contrario: "necesito que queden rectangulares, pero sin tanto espacio a la derecha ni abajo, estamos desperdiciando mucho el viewport", con una captura anotada mostrando un recuadro mucho más chico que el actual como referencia de proporción deseada. **Dos causas del espacio desperdiciado, dos fixes:** (1) el alto de card subido en D64 (260px/240px/230px según viewport) sobraba mucho una vez que el contenido es solo ícono+título corto — vuelve a 150px en las 3 vistas (desktop, tablet ≤800px, carrusel móvil ≤640px), cerca del valor que ya tenía D53 antes del desvío de D64. (2) el título tenía `max-width: 13ch` desde D45/D46 (pensado para controlar el wrap cuando la card era angosta) — con la card de vuelta a un ancho rectangular normal, ese tope dejaba el texto envuelto en 3 líneas angostas con un vacío visible a la derecha, exactamente lo que el Owner señaló en la captura; se quita el `max-width`, el título ahora usa el ancho real de la card y envuelve en menos líneas (ej. "Infraestructura para atención masiva." pasa de 3 líneas a 2). Verificado localmente (servidor estático en :4173) en desktop (1280px, 3 columnas), tablet (700px, 2 columnas) y el carrusel móvil (375px): las 6 cards se ven compactas y rectangulares, títulos usan el ancho disponible sin vacíos evidentes, colores de D64 sin cambios; sin errores de consola en ninguna vista. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/css/components.css`; revierte el alto de card de D64 (los colores de D64 siguen vigentes, sin cambios) |
| D66 | **"En cifras": se quita la leyenda "Cifra de ejemplo — pendiente de dato aprobado por el cliente" bajo cada número, y se agrega un ícono por tarjeta encima del número, relacionado con lo que mide.** El Owner pidió ambos cambios sobre una captura de la sección. La leyenda se había agregado en D41 específicamente para dejar explícito que los 3 valores (100, 50, $100M) son placeholders sin dato real aprobado — quitarla, con los números placeholder intactos, hace que el sitio muestre esas cifras como si fueran datos reales. **Antes de tocar código**, dado que esto es exactamente el tipo de situación que el criterio de "no inventar" del proyecto existe para prevenir, se confirmó con AskUserQuestion: se le explicó el riesgo explícitamente (las cifras seguirían siendo de ejemplo pero sin ningún aviso visible) y se presentaron 3 caminos — reemplazar por cifras reales si ya las tenía, quitar la leyenda de todas formas entendiendo el riesgo, o usar un aviso más discreto en vez de quitarlo del todo. **El Owner eligió quitar la leyenda de todas formas**, confirmando explícitamente que entiende que los números siguen siendo de ejemplo. Se quita el párrafo `.stat-card__pending` (y su regla CSS, sin más uso) de los 3 `.stat-card`, y se actualiza el `aria-label` de `.stats-grid` para no seguir anunciando "pendientes de dato aprobado" a lectores de pantalla cuando la versión visual ya no lo dice. **Íconos agregados** (SVG en el mismo estilo de línea del resto del sitio, viewBox 256, stroke-width 14): una carpeta para "Casos gestionados", una silueta de persona para "Clientes atendidos", una flecha ascendente ("trending up") para "Cartera recuperada" — sin inventar ningún dato, son solo ilustrativos del concepto que mide cada cifra. Nueva clase `.stat-card__icon` (40×40px, color `--color-accent-on-dark`, mismo tono que el número, centrado con `margin:0 auto`). Verificado localmente (servidor estático en :4173) en desktop (1280px) y mobile (375px): los 3 íconos renderizan correctamente encima de cada número, la leyenda ya no aparece en ninguna vista; sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/index.html` y `prototype/css/components.css`; reemplaza parcialmente D41 (se retira la leyenda de aviso, el resto — mecanismo de conteo, valores placeholder — sigue vigente) |
| D67 | **Footer: se agrega la columna "Sitio" (mapa del sitio) entre Contacto y Redes sociales — footer pasa de 3 a 4 columnas — y el footer completo de Home se replica idéntico en las 7 páginas reales, reemplazando el footer simple que tenían las 6 páginas interiores.** El Owner envió una captura del footer simple de las páginas interiores (el que ya existía, con columnas Marca/Sitio/Legal) y pidió "rescatar" esa columna "Sitio" para agregarla al footer más elaborado de Home (que solo tenía Marca/Contacto/Redes sociales, sin mapa del sitio), ubicándola entre Contacto y Redes; luego, mismo patrón que D55 con el header, replicar el resultado en todas las páginas. **Antes de tocar código**, se detectó que la captura mostraba nombres/páginas desactualizados respecto a la estructura vigente (D36 renombró "Servicios"→"Metodología" y "Nuestro Equipo"→"Quiénes somos"; "Nuestra Experiencia" no es una página real, redirige al stub `proximamente.html`; no existe una página "Contacto" separada, el flujo real es `captacion-b2b.html`) — se confirmó con AskUserQuestion qué lista armar: mantener el menú actual de 4 ítems del header, ampliarlo a un sitemap más completo con nombres actualizados, o que el Owner especificara los enlaces él mismo. **El Owner eligió el sitemap más completo.** Lista final: Inicio (`index.html`), Metodología (`servicios.html`), Quiénes somos (`nuestro-equipo.html`), Trabaja con nosotros (`nuestro-equipo.html#postulacion-heading`, mismo anchor que el 4º ítem del header), Blog (`noticias-analisis.html`, oculto del menú de header desde D36 pero página real) y Contacto (`captacion-b2b.html`, el flujo real de conversión). **Aplicado:** nueva sección `.home-footer__sitemap` con heading `.home-footer__heading` (mismo tratamiento visual — mayúsculas, gold-100, letter-spacing — que el `<h2>` del footer simple retirado) y lista `.home-footer__sitemap-list`; `.home-footer__grid` pasa de 3 a 4 columnas (`grid-template-columns` ajustado). El footer completo, antes exclusivo de Home vía `.home-page .site-footer`/`.home-page .home-footer__*`, se generaliza a `.site-footer--global` (mismo patrón que `.site-header--global` de D55) y se replica el HTML exacto en las 6 páginas interiores, reemplazando su footer simple (`footer-grid`/`footer-links`/`footer-bottom`, clases que siguen existiendo en el CSS porque `proximamente.html` y `type-lab.html` — fuera de alcance, mismo criterio que D34/D55 — todavía las usan). Verificado localmente (servidor estático en :4173) en las 7 páginas reales: footer de 4 columnas en desktop (≥1040px), 2×2 en tablet (851px) y apilado en mobile (375px), con los 6 enlaces correctos; `proximamente.html`/`type-lab.html` confirmados sin cambios (siguen sin la clase `site-footer--global`); sin errores de consola en ninguna página. | Owner, 2026-08-21 | Vigente — aplicado en las 7 páginas reales y `prototype/css/components.css`; reemplaza el footer simple (`footer-grid`/`footer-links`) de las 6 páginas interiores, que sigue vigente solo en `proximamente.html`/`type-lab.html` |
| D68 | **Bug de D67: el heading "Sitio" del footer se veía gigante solo en Home, correcto (chico) en las 6 páginas interiores.** El Owner señaló con una captura anotada que el tamaño no coincidía entre Home y el resto. Causa raíz: una regla genérica preexistente, `.home-page section:not(.hero-immersive) h2 { font-size: 2.3rem; ... }` (pensada para los títulos reales de sección de Home, ej. "Sectores de experiencia"), atrapaba también el nuevo `<h2 class="home-footer__heading">Sitio</h2>` de D67 porque vive dentro de un `<section>` — y por especificidad CSS (2 clases + 2 elementos) le ganaba a la regla correcta `.site-footer--global .home-footer__heading` (2 clases, 0 elementos). En las 6 páginas interiores esa regla nunca se activaba porque no tienen la clase `.home-page` en el `<body>` — por eso ahí sí se veía bien. **Fix:** se agrega `:not(.home-footer__section)` a esa regla genérica (y a su variante responsive en el mismo archivo), excluyendo explícitamente la sección del footer sin tocar el resto de títulos de sección de Home. Verificado localmente (servidor estático en :4173): el heading "Sitio" mide 13px en Home (antes 2.3rem ≈ 37px), igual que en las páginas interiores; el resto de los `<h2>` de sección en Home (ej. "Sectores de experiencia") confirmado sin cambios (36.8px); sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/css/components.css` |
| D69 | **Servicios § "Nuestra metodología" (flujograma de D48): la descripción de los 3 pasos queda siempre visible en desktop también (antes solo con hover/foco), y los 3 nodos se alinean con el margen izquierdo del eyebrow/título en vez de quedar centrados y distribuidos por todo el ancho.** El cliente pidió (vía el Owner) ambos cambios sobre el resultado en vivo del flujograma. El popover-al-hover de D48 (mismo patrón de D47) existía justamente para no recortar el copy real sin necesitar scroll junto a los otros 2 pasos — el cliente prefiere ahora que el texto esté siempre a la vista, aceptando que la sección ocupe más alto. **Aplicado en `prototype/css/components.css`, solo dentro de `@media (min-width: 801px)` de `.stepper--flowchart`** (la vista mobile/tablet ≤800px ya mostraba la descripción siempre visible desde D48, sin cambios ahí): se elimina el bloque de popover absoluto (`position:absolute`, `opacity:0` en reposo, revelado por `:hover`/`:focus-within`) y el bloque paralelo que ya mostraba el texto en flujo normal solo para touch/no-hover — ahora un único tratamiento se aplica siempre, con el párrafo en flujo normal bajo el título. `.stepper__step` pasa de `align-items:center; text-align:center` a `align-items:flex-start; text-align:left`; `.stepper__step-head` dentro de este breakpoint deja de forzarse a columna (ícono encima del título) y hereda el mismo layout de fila (ícono junto al título) que ya usa la vista mobile/tablet desde D49; se agrega `.stepper__step:first-child { padding-left: 0 }` para que el ícono del primer paso quede exactamente en el mismo margen izquierdo que el eyebrow/título de la sección (antes el `padding` uniforme de los 3 pasos, combinado con `text-align:center`, dejaba el primer ícono centrado dentro de su columna en vez de pegado al borde). Verificado localmente (servidor estático en :4173) en desktop (1280px): la descripción de los 3 pasos se ve siempre, sin necesitar hover; el ícono de "Diagnóstico" alinea exactamente con el borde izquierdo de "NUESTRA METODOLOGÍA" y el título. Confirmado que la vista tablet (700px) y el carrusel móvil (375px) no cambiaron (ya mostraban la descripción siempre visible, apiladas verticalmente, desde D48/D49/D50). Sin errores de consola en ninguna vista. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/css/components.css`; reemplaza parcialmente D48 (el popover-al-hover en desktop, solo en esta sección — Captación B2B § "Nuestro proceso" no se ve afectada) |
| D70 | **Servicios § "Nuestros servicios": el recuadro de imagen de los 3 bloques baja de 280px a 220px de alto mínimo — se veía demasiado alto, desperdiciando viewport.** El Owner señaló con una captura anotada el espacio vacío arriba y abajo del texto en cada bloque, generado por el recuadro de imagen (`.service-block__media`) siendo más alto de lo necesario — pidió reducirlo, aceptando explícitamente que quede cuadrado en vez de mantener una proporción más ancha que alta. Se baja `min-height` de `280px` a `220px` — el extremo angosto del rango `minmax(220px, 320px)` que ya tenía la columna de imagen, quedando cuadrado o casi cuadrado según el ancho real que tome la columna en cada viewport. Sin cambios en el resto del bloque (texto, tag, alternancia imagen izquierda/derecha) — pedido explícito del Owner de no tocar nada más. Verificado localmente (servidor estático en :4173): los 3 recuadros se ven visiblemente más compactos y cercanos a cuadrados en desktop (1280px), sin espacio vacío evidente alrededor del texto; mobile (375px, columna única) sin cambios de comportamiento. Sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/css/components.css` |
| D71 | **Corrige D70: el `min-height:220px` no tenía ningún efecto visible — el recuadro de imagen seguía viéndose igual de alto que antes.** El Owner reportó que no veía el cambio aplicado en el sitio en vivo. Investigado: la clase base `.media-placeholder` (que `.service-block__media` también lleva en el HTML, `class="media-placeholder service-block__media"`) fija `aspect-ratio: 4 / 5` — con el ancho real de la columna (320px), eso da 400px de alto, muy por encima de los 220px de `min-height`, así que el aspect-ratio ganaba siempre (min-height solo actúa como piso cuando nada más determina un alto mayor). Confirmado en el CSS servido en producción que el `min-height:220px` de D70 sí se había desplegado correctamente — el problema no era el deploy, era que esa propiedad nunca fue la que controlaba el alto real. **Fix real:** se agrega `aspect-ratio: 1 / 1` a `.service-block__media`, con la misma especificidad que la regla base pero declarada después en el archivo, así que gana por orden de cascada — ahora sí controla el alto real, quedando un cuadrado perfecto en vez de depender de un `min-height` que nunca aplicaba. Verificado localmente (servidor estático en :4173) con `getBoundingClientRect()`: el recuadro mide 320×320px en desktop (1280px, antes 320×400px); visualmente los 3 bloques se ven notablemente más compactos, sin el espacio vacío señalado por el Owner. Mobile (375px): recuadro cuadrado a ancho completo del contenedor. Sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/css/components.css`; corrige D70 (que no tenía efecto real) |
| D72 | **Nueva página `trabaja-con-nosotros.html`: las secciones "Nuestro equipo" y "Postulación" se mueven ahí desde `nuestro-equipo.html` (Quiénes somos), que las contenía hasta ahora — el 4º ítem del menú de header ("Trabaja con nosotros", desde D36) deja de ser un ancla dentro de Quiénes somos y pasa a apuntar a su propia página real.** El Owner pidió el movimiento explícitamente, y recordó mantener la misma distribución de header (D55) en la página nueva. **Contenido movido sin cambios de copy:** las secciones "Nuestro equipo" (equipo multidisciplinario, con la nota de contenido pendiente del cliente sobre fotos/organigrama/perfil de Jaime Marmolejo) y "Postulación" (formulario con nombre/correo/CV/campo adicional, CTA "Postúlese" — sigue siendo secundario y estrictamente contextual, D17, sin CTA comercial en esta página, mismo outline de `content/05-talento-aaa.md`) pasan tal cual a la página nueva. **Hero nuevo para la página:** eyebrow "Trabaja con nosotros" + h1 que reutiliza la línea de apertura ya aprobada en C1 para la extinta página "Talento AAA" ("Un lugar donde el rigor técnico se construye entre personas, no solo entre procesos.", `content/05-talento-aaa.md`) — no se inventa copy nuevo de cultura/beneficios, que sigue pendiente de insumo del cliente (mismo vacío ya documentado en ese archivo). **`nuestro-equipo.html`** conserva Hero/Quiénes somos/Filosofía/Cierre (LinkedIn/Instagram), sin cambios de copy en lo que queda. **Replicación en las 7 páginas reales** (mismo patrón que D55/D67): el header y el footer de la página nueva son una copia exacta de las otras 6; en las 7 páginas (incluida la nueva), el ítem de menú y el enlace del footer "Trabaja con nosotros" pasan de `nuestro-equipo.html#postulacion-heading` a `trabaja-con-nosotros.html` — en la página nueva ese ítem lleva `aria-current="page"` (ya es una página real, no un ancla), en las demás no. Comentarios HTML desactualizados sobre el ancla (en `nuestro-equipo.html` y en la nota canónica de D36 en `index.html`) se actualizan para reflejar el cambio. Verificado localmente (servidor estático en :4173): la página nueva carga con header/footer idénticos a las demás, "Trabaja con nosotros" resaltado como activo en el menú, "Nuestro equipo" y "Postulación" renderizan correctamente (formulario incluido); `nuestro-equipo.html` sin huecos ni artefactos donde estaban las secciones movidas, transición directa de "Filosofía" al cierre de redes sociales; confirmado en `captacion-b2b.html` que el menú y el footer ya apuntan a la página nueva; conteo de `<div>`/`</div>` balanceado en las 8 páginas tocadas; mobile (375px) sin problemas; sin errores de consola en ninguna vista. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/trabaja-con-nosotros.html` (nueva), `prototype/nuestro-equipo.html` y el header/footer de las 6 páginas reales restantes; reemplaza parcialmente D36 (el 4º ítem del menú deja de ser un ancla) |
| D73 | **Servicios § "Transparencia" reusa el diseño de Home § "¿Qué hace AAA?" (dos columnas, fondo oscuro, texto + recuadro de imagen con patrón rayado), en vez del bloque de dos tarjetas con halo que tenía; se quita la sección "¿Su empresa tiene un crédito en riesgo o en proceso de insolvencia?" y se reemplaza por el mismo cierre de Home, "Conversemos sobre su caso.".** El Owner pidió los tres cambios juntos, con una captura de la sección de Home como referencia de diseño para "Transparencia". **Transparencia:** se reemplaza el `.cierre-grid` de dos tarjetas (imagen + card con halo) por `.grid-2.what-we-do-grid` + `.media-placeholder.what-we-do-visual` — exactamente la estructura de "¿Qué hace AAA?" en Home. El copy no cambia (mismo texto, mismo `data-img-id="IMG-13"`); solo cambia la estructura visual y el fondo (de claro a oscuro, siguiendo el diseño de referencia). Las reglas CSS `.what-we-do-grid`/`.what-we-do-visual`, antes exclusivas de Home vía `.home-page` (mismo patrón que D55/D67 con header/footer), se generalizan quitando ese prefijo — confirmado que Home no se ve afectada. **Cierre:** se retira por completo la sección "¿Su empresa tiene un crédito en riesgo...?" (con su CTA "Consulte su caso con ADY" hacia `#nota-ady`) y se reemplaza con el bloque de cierre real de Home — mismo HTML, mismo copy ("Conversemos sobre su caso.", con el párrafo de acompañamiento), mismo destino del CTA "Hablemos de su caso" (`captacion-b2b.html`) — sin inventar variante nueva, una reutilización fiel. Verificado localmente (servidor estático en :4173) en desktop (1280px) y mobile (375px): "Transparencia" se ve idéntica al diseño de referencia de Home; el nuevo cierre reemplaza al anterior sin dejar huecos; Home confirmado sin cambios visuales en su propia sección "¿Qué hace AAA?"; sin errores de consola en ninguna vista/página. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/servicios.html` y `prototype/css/components.css`; generaliza reglas antes exclusivas de Home (D26/histórico) |
| D74 | **Corrige D73: el cierre "Conversemos sobre su caso." reusado en Servicios no coincidía visualmente con el de Home — degradado vs. color sólido, halo visible vs. oculto, botón blanco vs. dorado, h2 más chico.** El Owner señaló la diferencia con una captura de Home como referencia y pidió que se vea idéntico, "esto incluye texto y CTA, además de ese efecto de degradado". **Causa:** el tratamiento visual de `.cierre-card` que da a Home su apariencia distintiva (degradado radial+lineal, halo oculto, h2 de 2.3rem sin recorte de 12ch, botón `--color-accent-main`) vivía enteramente bajo `.home-page`, y D73 solo reutilizó las clases base (`.cierre-card`, `.cierre-card--cta`, etc.), que caen al estilo genérico —distinto— cuando no hay `.home-page` en el `<body>`. **Fix, con cuidado de no romper Captación B2B:** `.cierre-card` (y `.cierre-card__content h2` en particular) también lo usa la tarjeta de cierre propia de Captación B2B, con un encabezado mucho más largo ("Cuéntenos su caso y le contactamos con un diagnóstico inicial") — generalizar sin más el estilo de Home (como en D55/D67/D73) le habría forzado `max-width:12ch`/`font-size:2.3rem`, rompiendo ese encabezado. En su lugar se crea un modificador dedicado, `.cierre-grid--global` (todas las reglas de Home ahora viven bajo `.cierre-grid--global .cierre-card*` en vez de `.home-page .cierre-card*`, incluidas las dos variantes responsive que estaban mezcladas con selectores de Home más generales), y se agrega esa clase únicamente al `.cierre-grid` de Home y al nuevo de Servicios — Captación B2B no la lleva y conserva su tratamiento original sin cambios. Verificado localmente (servidor estático en :4173): el cierre de Servicios ahora es visualmente idéntico al de Home (degradado, sin halo, botón dorado, h2 grande) en desktop (1280px) y mobile (375px); Home confirmado sin cambios; Captación B2B confirmado sin cambios (color sólido, halo visible, botón blanco, encabezado largo sin recortar); sin errores de consola en ninguna página. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/index.html`, `prototype/servicios.html` y `prototype/css/components.css`; corrige D73 |
| D75 | **Quiénes somos: la sección "¿Quiénes somos?" recicla el diseño de Home § "¿Qué hace AAA?" para corregir un desajuste de márgenes con Inicio/Metodología; "Filosofía" se rediseña de una línea de texto plana a 3 tarjetas de atributo; se retira el cierre "Conócenos también en LinkedIn e Instagram.".** El Owner pidió los tres cambios juntos, con la sección de Home como referencia de diseño para el primero. **"¿Quiénes somos?":** el contenedor tenía `max-width: 68ch` propio (en vez del contenedor estándar del sitio), lo que corría el margen izquierdo del contenido respecto a Inicio/Metodología — se reemplaza por la misma estructura de "¿Qué hace AAA?"/"Transparencia" (`.grid-2.what-we-do-grid` + `.media-placeholder.what-we-do-visual`, generalizadas en D73/D74), pasando de fondo "atmósfera" a fondo oscuro plano, con la nota `.ady-note` de founders pendientes (D19) movida dentro de la columna de texto. Sin cambios de copy. **Filosofía:** "muy plana" y con el mismo problema de márgenes — se reemplaza la única línea de texto corrida ("Innovación con rigor. Solidez con tecnología. Dimensión humana.") por 3 tarjetas de atributo (mismos 3 pilares, sin cambios de copy, cada uno ahora con su propio ícono y título), en vez del texto en una sola oración. Nueva clase `.philosophy-grid`/`.philosophy-card`, con el mismo lenguaje visual ya usado en `.sector-card` (borde, `--radius-md`, ícono + título Krub bold) — no se reutiliza/generaliza `.focus-grid` de Home porque ese componente está pensado para 6 tarjetas con ciclo de color cada 3, no para 3 tarjetas parejas. Contenedor estándar (sin `max-width` propio), resolviendo el mismo problema de margen. Heading pasa de `<h3>` a `<h2>` (consistente con el resto de secciones de nivel superior de la página). **Cierre:** se retira por completo la sección "Conócenos también en LinkedIn e Instagram." — la página termina en Filosofía, seguida directo del footer. Verificado localmente (servidor estático en :4173) en desktop (1280px) y mobile (375px): "¿Quiénes somos?" alinea con el margen del hero/Inicio/Metodología; Filosofía se ve como 3 tarjetas con íconos, apiladas en mobile; el cierre de redes ya no aparece, transición directa a footer; sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/nuestro-equipo.html` y `prototype/css/components.css` |
| D76 | **Se quita el aviso "Founders — contenido pendiente del cliente" de "¿Quiénes somos?"; se normaliza `.text-gradient` en todo el sitio al color dorado sólido de Home, quitando el degradado (no aprobado por el cliente).** El Owner pidió ambos cambios en el mismo mensaje. **Aviso de founders:** se retira por completo el `.ady-note` correspondiente — Decisión D19 (no inventar biografía de los founders sin insumo del cliente) sigue vigente, solo se quita el aviso visible en pantalla. **Texto en cursiva del H1 (`.text-gradient`, usado en Inicio, Metodología y Captación B2B — "Un H1 por página", máx. 2-3 usos):** el Owner señaló que el estilo de este texto en Home no coincidía con el de las demás páginas — investigado, la causa era que `.home-page .text-gradient` sobreescribía la regla base con un color dorado sólido (`--color-accent-main`) solo en Home, mientras que las demás páginas (sin la clase `.home-page` en el `<body>`) caían en la regla base, que sí aplicaba un degradado real (`background-clip:text`) nunca aprobado por el cliente. Se pidió normalizar todo el sitio al tratamiento de Home. **Aplicado:** la regla base `.text-gradient` se reescribe para usar directamente el color sólido `--color-accent-main` (mismo dorado que ya tenía Home) en vez del degradado; se quita por completo el degradado alternativo para fondos claros (ahora usa `--color-navy-900` sólido, mismo criterio de contraste); se elimina el override `.home-page .text-gradient` (ya redundante, la regla base ahora coincide exactamente) y el bloque `@supports not (background-clip: text)` (ya sin sentido, no hay más `background-clip`). El nombre de la clase (`.text-gradient`) se conserva sin cambios para no tocar el HTML de las 3 páginas que la usan. Verificado localmente (servidor estático en :4173) en Inicio, Metodología, Captación B2B y mobile (375px): el texto en cursiva ("somos la voz del acreedor.", "su crédito?", "proteger, normalizar y recuperar activos") se ve idéntico — mismo dorado sólido, sin degradado — en las 3 páginas; sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/nuestro-equipo.html`, `prototype/css/components.css` y `prototype/css/tokens.css` (comentario); reemplaza D26 (degradado) en las páginas que no son Home |
| D77 | **Quiénes somos y Trabaja con nosotros no tenían el énfasis en cursiva dorada (`.text-gradient`) que sí tienen los demás heroes del sitio — se agrega a las dos; en Metodología, "su" y "crédito?" se unen con un espacio irrompible para que el texto dorado quede siempre en el mismo renglón.** El Owner señaló, con una captura de Metodología, que "su" quedaba suelto al final de la primera línea (junto al texto blanco) mientras "crédito?" caía solo en la segunda — pidió bajar "su" para que las dos palabras del texto dorado se leyeran juntas. De paso, notó que los heroes de Quiénes somos y Trabaja con nosotros no tenían ningún fragmento en cursiva dorada, a diferencia de Inicio/Metodología/Captación B2B, y pidió que **todos los headers de páginas nuevas o existentes usen el mismo estilo de ahora en adelante** — se registra como criterio de estilo para páginas futuras, no solo un parche puntual. **Metodología:** se une `su crédito?` con `&nbsp;` dentro del `<span class="text-gradient">` — con las dos palabras como unidad indivisible, el navegador ahora manda el bloque completo a la segunda línea en vez de partirlo, en cualquier ancho de viewport (más robusto que un `<br>` fijo). **Quiénes somos:** el H1 ("Sobre nosotros: AAA Estudio Legal") gana la clase `hero__title` (que le faltaba — controla el margen inferior estándar del hero) y envuelve "AAA Estudio Legal" en `.text-gradient`. **Trabaja con nosotros:** mismo tratamiento — el H1 gana `hero__title`, y se envuelve el tramo final ("entre personas, no solo entre procesos.") en `.text-gradient`, el mismo patrón de la frase contrastante que ya usan los demás heroes narrativos del sitio. Sin cambios de copy en ningún caso — solo se envuelve texto ya existente en el span de énfasis. Verificado localmente (servidor estático en :4173) en desktop (1280px) y mobile (375px): "su crédito?" queda siempre en un mismo renglón en ambas vistas; "AAA Estudio Legal" y "entre personas, no solo entre procesos." se ven en dorado cursiva, igual que el resto de heroes del sitio; sin errores de consola en ninguna de las 3 páginas. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/servicios.html`, `prototype/nuestro-equipo.html` y `prototype/trabaja-con-nosotros.html`; establece el criterio de estilo para el hero de cualquier página futura |
| D78 | **Segundo rediseño de "Filosofía": las 3 tarjetas de atributo de D75 no convencieron — se reemplazan por el patrón ya construido foto + tarjeta sólida (`.cierre-grid`/`.cierre-card`), con una referencia visual externa como inspiración de composición (no de paleta/tipografía). Corrige además el tamaño del H1 en Quiénes somos/Trabaja con nosotros, que no coincidía con Inicio/Metodología.** El Owner adjuntó una captura de un sitio de referencia (foto de fondo + tarjeta oscura flotante con ícono, título, párrafo y CTA) y pidió recrear esa composición, aclarando explícitamente usar nuestra propia paleta y tipografía, no las de la referencia (verde oscuro, otra fuente). **Filosofía, aplicado en `prototype/nuestro-equipo.html`:** se retira por completo el diseño de D75 (`.philosophy-grid`, 3 tarjetas separadas) y se reutiliza el patrón `.cierre-grid` + `.cierre-card--image` (`.media-placeholder`) + `.cierre-card--cta` con `.cierre-card__content--statement` — el mismo tratamiento que ya tenía "Transparencia" en Servicios antes de D73 (foto a un lado, tarjeta navy sólida con halo decorativo al otro) — en vez de inventar un componente nuevo de foto+tarjeta superpuesta. Vuelve el copy original de Filosofía en un solo párrafo, sin cambios ("Innovación con rigor. Solidez con tecnología. Dimensión humana..."), reemplazando las 3 tarjetas separadas de D75. Se agrega un ícono de brújula (círculo + aguja) arriba del título, como en la referencia, coloreado con `--color-accent-light` (mismo tono que usa el eyebrow en este tipo de tarjeta). Las clases `.philosophy-grid`/`.philosophy-card` de D75 se eliminan del CSS por quedar sin ningún uso. **H1 de Quiénes somos/Trabaja con nosotros, aplicado en `prototype/css/components.css`:** el Owner señaló que el tamaño de esos dos títulos no coincidía con Inicio/Metodología — investigado: esas dos páginas usan un hero simple sin el wrapper `.hero-shell`, así que su `.hero__title` nunca recibía el `font-size` de `.hero-shell__content .hero__title` (`--text-2xl`, 3rem/48px) y caía al tamaño por defecto de cualquier `<h1>` (`--text-3xl`, 3.75rem/60px, más grande). Se fija `font-size: var(--text-2xl)` directamente en la clase base `.hero__title`, para que cualquier hero (con o sin `.hero-shell`) use el mismo tamaño por defecto de ahora en adelante — las reglas más específicas de `.hero-shell__content .hero__title`/`.home-page .hero-shell__content .hero__title` siguen ganando donde ya existían, sin cambios en Inicio/Metodología/Captación B2B. Verificado localmente (servidor estático en :4173) con `getComputedStyle`: las 4 páginas con hero (Inicio, Metodología, Quiénes somos, Trabaja con nosotros) miden exactamente 48px de font-size en el H1; Filosofía se ve como foto + tarjeta con ícono/título/párrafo, coherente con la línea gráfica del sitio; verificado en desktop (1280px) y mobile (375px); sin errores de consola en ninguna página. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/nuestro-equipo.html` y `prototype/css/components.css`; reemplaza D75 (la parte de Filosofía; "¿Quiénes somos?" y el resto de D75 no se tocan) |
| D79 | **Home § "Sectores de experiencia": se ensancha la columna derecha del encabezado (párrafo + CTA) para que quede más pareja en alto con la columna izquierda (eyebrow + título); se fusionan Financiero+Fintech en una sola tarjeta y Salud+Cajas de compensación en otra — el sitio vuelve a 6 tarjetas en vez de 8.** El Owner señaló, con una captura anotada, que el párrafo de la derecha ("Trabajamos con organizaciones...") era más alto que el título/eyebrow de la izquierda — con `align-items: end` en el grid del encabezado, eso dejaba un vacío visible arriba del eyebrow. **Encabezado, aplicado en `prototype/css/components.css`:** la columna derecha (`.sector-experience__intro`) era demasiado angosta para su propio texto (`minmax(280px, 0.7fr)`, `max-width: 42ch`), forzándolo a envolver en más líneas de las necesarias — se ensancha a `minmax(340px, 0.95fr)` (columna izquierda ajustada de 1.45fr a 1.2fr en proporción) y `max-width: 50ch`, con lo que el párrafo envuelve en menos líneas y ambas columnas quedan visualmente más parejas. **Fusión de tarjetas, aplicado en `prototype/index.html`:** el cliente pidió (vía el Owner) fusionar Financiero+Fintech en una sola tarjeta y Salud+Cajas de compensación en otra, "con el objetivo que queden las 6 cards que teníamos antes" (antes de la expansión a 8 en D62) — conservando explícitamente los íconos de Financiero y de Salud (se retiran los de Fintech y Cajas de compensación). "Financiero y Fintech" queda con los logos de ambos sectores juntos (BBVA, Femcristar, Nuestro Fondo); "Salud y cajas de compensación" con los suyos (Promédico, Comfandi). Solidario, Sector público, Construcción e inmobiliario y Comercio y consumo (los 2 sectores nuevos de D62) no se tocan — el sitio queda con 6 tarjetas: Financiero y Fintech, Solidario, Sector público, Salud y cajas de compensación, Construcción e inmobiliario, Comercio y consumo. El párrafo de intro de la sección no se modifica (sigue nombrando los sectores individualmente — financiero, fintech, salud, cajas de compensación — como categorías reales que la firma atiende, independiente de cuántas tarjetas visuales las agrupen). Verificado localmente (servidor estático en :4173) en desktop (1280px) y mobile (375px, carrusel): el encabezado se ve más parejo en alto; las 6 tarjetas renderizan con los logos e íconos correctos, sin duplicados ni huecos; sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/index.html` y `prototype/css/components.css`; revierte parcialmente D62 (número de tarjetas, no el mapeo de logos a sector subyacente) |
| D80 | **Home § "Sectores de experiencia": los títulos de las 6 tarjetas bajan un punto de tamaño.** El Owner pidió reducir el tamaño de los títulos ("Financiero y Fintech", "Solidario", etc.), sugiriendo 1.3rem. `.sector-card__name` pasa de `clamp(1.15rem, 1.6vw, 1.5rem)` (tope 1.5rem en desktop) a `1.3rem` fijo. Verificado localmente (servidor estático en :4173) con `getComputedStyle`: 20.8px (1.3rem) confirmado; sin errores de consola. | Owner, 2026-08-21 | Vigente — aplicado en `prototype/css/components.css` |

*(Autoría del blog **sigue pendiente**, sin decisión — ver STATUS.md. No bloquea C0.)*

## Ficha C0 (candidata — Sesión 01, pendiente de validación Product Lead)

> No autoaprobada. Producida por Sesión 01 a partir de AGENTS.md, PROJECT.md, REFERENCES.md, REFERENTS.md, STATUS.md y QA.md. Sintetiza por referencia — no reproduce las secciones ya desarrolladas arriba.

| Dimensión | Síntesis | Detalle |
|---|---|---|
| Negocio | Rediseño de agudeloabogados.com como materialización de un reposicionamiento de marca (de generalista/antigua a especializada/madura/tecnológica), bajo "AAA Estudio Legal" | [Resumen](#resumen) |
| Objetivo | **Actualizado (D15):** principal = reconocimiento de marca y showcase de servicios; secundario = atracción de talento (invierte la lectura previa). Cerrado con Conversión (D17) — sin tensión | [Objetivo](#objetivo) |
| Usuarios | **Actualizado (D15):** primario = mercado/clientes potenciales y corporativos; secundario = talento jurídico potencial | [Audiencia](#audiencia) |
| Conversión | **Cerrado (D17/D18):** principal = captar empresas (D9, formulario) con ADY como mecanismo destacado ("Consulta tu caso aquí", D18); secundario y contextual = formulario de talento (D8), solo en Talento AAA | [Conversión](#conversión) |
| Alcance incluido | Hasta 6 secciones + página legal (D2), WordPress administrable, hasta 2 CPT (diferido, D3), hasta 6 banners, hasta 3 formularios, SEO/GEO, UX Writing, 2 posts de blog, migración de hosting, Polylang, GA4/GTM, capacitación 1h | [Alcance](#alcance) |
| Alcance excluido | Integraciones ERP/CRM, funcionalidades a medida, copy extenso (ver D5), banco de imágenes/video, plugins adicionales, mantenimiento post-lanzamiento, animaciones de scroll detalladas (diferidas a C2, D7), CPTs definidos (diferido a 03A, D3) | [Alcance](#alcance) |
| Hechos confirmados | Marca, colores (`#063B59` + gama dorada), tipografías (Big Caslon/Krub), stack actual (WordPress+Elementor+LiteSpeed), moneda COP (D4) | [STATUS.md → Hechos confirmados](STATUS.md) |
| Hipótesis | "Hasta 4 casos de éxito" consistente entre slides pero ausente de la tabla de especificaciones técnicas — no confirmado como compromiso duro | [STATUS.md → Hipótesis](STATUS.md) |
| Vacíos (no bloquean C0) | ✅ Dirección resuelta (D14). Quedan: autoría del blog; **crítico:** casi todo el contenido de Talento AAA sin responder; casos de éxito estructurados sin llenar; estatus del párrafo narrativo de servicios sin aclarar; **nuevo:** orden de conversión sin reconfirmar tras D15 | [STATUS.md → Vacíos pendientes](STATUS.md) |
| Riesgos principales | (1) 7ª y 8ª página (legal, B2B) como posible fricción contractual; (2) filtración de las 14 áreas de práctica hacia los 3 macroservicios vigentes (D24); (3) ambición visual vs. factibilidad WordPress (D7); (4) D9 (B2B como conversión terciaria) sigue sin sustento directo del cliente — pendiente Product Lead; (5) vacío crítico de contenido en Talento AAA | [STATUS.md → Riesgos](STATUS.md) |
| Sitemap preliminar | 8 páginas: Inicio, Talento AAA, Capacidad Técnica, Nuestro Equipo, Nuestra Experiencia, Noticias y Análisis, Página Legal (D2, fuera de conteo), Captación B2B (D11, **no contratada**, pendiente Product Lead/Change Request) + elemento transversal ADY (CTA) | [Sitemap](#sitemap) |

### Contradicciones — vendido / pedido / disponible

| Eje | Vendido (propuesta) | Pedido (brief) | Disponible (brochure / sitio actual) | Tensión y estado |
|---|---|---|---|---|
| N.º de páginas | "Hasta 6 secciones" (slide 6) | No amplía | — | 7ª página legal autorizada fuera del conteo (D2); 8ª página de captación B2B (D11) **no contratada**, probable Change Request. Ambas pendientes de confirmación con Product Lead/Uhura — abierta |
| Conversión / audiencia | No define acción de conversión | Prioriza talento sobre lead comercial directo (interpretación previa) | — | ✅ Resuelta (D17, Owner, 2026-07-23): conversión principal = empresas (B2B); talento es secundario y estrictamente contextual. Cierra contradicciones #6 y #10 |
| Áreas de práctica | No fija número | D24 reduce el relato a 3 macroservicios (Defensa de acreedores en insolvencia, Normalización de Activos, Corporativo); Concursal queda integrado en el primero | Brochure y sitio actual listan 14 áreas | Riesgo de filtración si el brochure se usa sin aplicar el filtro acordado (STATUS.md, contradicción #5) |
| Extensión de copy | Excluye "copy extenso" (slide 7) | Pide reescritura completa / "UX Writing" (slide 6, pregunta 5) | — | Resuelta (D5): extensión por criterio narrativo/escaneabilidad, no por regla fija de longitud |
| CPTs | Techo hasta 2 CPT / 6 campos c/u | No menciona | — | Diferido a 03A (D3) |
| Datos de contacto | No define | No define | Brochure: Oficina 701; sitio actual: Oficina 403; brief ampliado: Calle 28 Norte #6BIS-26 | ✅ Resuelta (D14, Owner, 2026-07-23): dirección definitiva = Calle 28 Norte #6BIS-26, Barrio San Vicente, Cali |
| Identidad de marca | No define visualmente | Marca "AAA Estudio Legal" (2024) | Sitio actual publica "Agudelo Abogados Asociados" | No es contradicción a resolver — es la evidencia que justifica el proyecto |
| Timeline | 3 cifras distintas entre slides 7/10/11 (4–6 sem. / 4 sem. / 6 sem. laborables) | No menciona | — | Abierta, no bloquea C0 (STATUS.md, contradicción #3) |

**Estado de la ficha:** ✅ Aprobada (STATUS.md, 2026-07-23). Base congelada para C1 — cambios posteriores se registran como nuevas decisiones (D14+), no como reapertura silenciosa.

## Ficha C1 (candidata — pendiente de QA de Content y consolidación de Product Lead)

> Base: C0 aprobado (STATUS.md, 2026-07-23). No es diseño ni código — solo narrativa, sitemap, referentes y marco técnico preliminar. No se copió identidad ni código de los referentes. Contenido extenso por página vive en [/content/](content/); aquí solo se sintetiza y se apunta.

### Sitemap propuesto (propósito y CTA distintos por página)

| # | Página | Propósito | CTA principal |
|---|---|---|---|
| 1 | Inicio | Reconocimiento de marca; presentar de inmediato la conversión principal | "Consulta tu caso aquí" (D18, vía ADY) |
| 2 | Capacidad Técnica (Servicios) | Profundidad técnica en los 3 macroservicios, con representación exclusiva del acreedor explícita (D24) | "Consulta tu caso aquí" (D18) |
| 3 | Nuestra Experiencia (Clientes) | Prueba social editorial, sectores y casos | "Ver casos" (enlaza suave a la conversión principal) |
| 4 | Nuestro Equipo | Solidez humana — founders y equipo | Enlaces a LinkedIn/perfiles (sin CTA comercial) |
| 5 | Talento AAA | Cultura genuina; formulario **secundario y contextual** (D17) — no aparece fuera de esta página | "Postúlate" (solo aquí, nunca global) |
| 6 | Captación de empresas (insolvencia) | Comprador B2B, objeciones resueltas — acción **principal** (D17); página **no contratada** (D11) | "Consulta tu caso aquí" / formulario nativo de empresas |
| 7 | Noticias y Análisis (Blog) | SEO/GEO, especialización técnica | "Lee el análisis completo" |
| 8 | Página Legal | Cumplimiento (D2) — sin outline de contenido | Ninguno |
| — | ADY (transversal, D18) | Mecanismo destacado de la conversión principal | "Consulta tu caso aquí" → URL externa (a compartir) |

**Nota de jerarquía (D17/D18):** el CTA "Consulta tu caso aquí" es el mismo mensaje en todas sus apariciones (Inicio, Servicios, ADY) — es la conversión principal del sitio. El formulario de Talento AAA usa un CTA distinto ("Postúlate") y nunca compite por el mismo espacio visual que el CTA principal.

Outline por página, insumos y vacíos específicos: ver [/content/](content/) (archivos 00–08 y 09).

### Narrativa global (resumen — detalle en [content/00-narrativa-global.md](content/00-narrativa-global.md))

Mensaje central: AAA Estudio Legal representa exclusivamente al acreedor — no al deudor insolvente — con rigor técnico y tecnología visible. Su portafolio se articula en tres macroservicios: Defensa de acreedores en procesos de insolvencia, Normalización de Activos y Derecho Corporativo (D24). **Jerarquía de conversión (D17/D18):** captar empresas es la acción principal, con ADY ("Consulta tu caso aquí") como su mecanismo destacado; talento es secundario y solo aparece en su propio contexto. Tensiones de tono a vigilar: "normalización de activos" premium (no cobranza), masividad sin perder calidad.

### Matriz de referentes

| Referente | Rasgo útil | Límite | Aplicación en este proyecto | Riesgo |
|---|---|---|---|---|
| Visual 01 — Autoridad | Hero oscuro con prueba social e indicadores integrados; cards editoriales para servicios | Dorado solo acento, sin saturar; no caer en tono "abogado independiente" | Hero de Inicio (con foto real); cards de los 3 macroservicios en Capacidad Técnica | Inventar cifras/certificaciones si no hay datos reales aprobados |
| Visual 02 — Sistema modular | Retícula modular, cards con distinto peso, hover states claros | No convertir todo en dashboard; evitar exceso de cards pequeñas | Grid de los 3 macroservicios y de beneficios/capacidades | Restar seriedad institucional por exceso de lenguaje "startup" |
| Visual 03 — Elegancia institucional | Hero fotográfico con overlay, transición curva, equipo con fotografía vertical | Sin balanzas/mazos/iconografía jurídica genérica; dorado opaco, no metálico | Nuestro Equipo (founders + equipo); transición hero→contenido en Inicio | Caer en cliché de "bufete tradicional" con fotografía de stock |
| OlarteMoure — carrera-om | Cultura contada por personas reales, no enunciada | Requiere testimonios reales, no se puede simular | Talento AAA — pero **no ejecutable todavía** (sin testimonios ni respuestas del cliente) | Inventar testimonios o cultura no confirmada |
| Kreisson — scrollytelling progresivo | Narrativa que evoluciona con el scroll | Diferido a C2 (D7); requiere JS mínimo y fallback | Candidato para Capacidad Técnica o Inicio | Sobrecargar performance/accesibilidad si se implementa sin cuidado |
| Sheff&Cook — sticky crossfade | Contenido fijo + imagen que cambia por bloque activo | Diferido a C2 (D7); requiere `prefers-reduced-motion` y fallback sin JS | Candidato para Capacidad Técnica (los 3 macroservicios) | Igual que arriba — no se implementa hasta que C2 confirme factibilidad en WordPress |

### Principios visuales/narrativos propios (traducidos de los referentes, no copiados)

1. Autoridad editorial, no comercial genérica: contenido real (casos, experiencia, cifras aprobadas) antes que adjetivos vagos.
2. Alternancia clara/oscura como ritmo de marca, no decoración — oscuro para momentos de autoridad, claro para lectura pausada.
3. Sistema modular consistente (retícula + cards de distinto peso) que se sienta digital sin perder seriedad institucional.
4. Fotografía y voz humana genuina (equipo real, sin stock genérico ni testimonios inventados) como prueba de solidez.
5. Tecnología como trasfondo narrativo (ADY representa innovación/robustez, D18) que ahora también actúa como el CTA de conversión más visible del sitio — no se explica qué es ADY, pero sí se hace evidente que hay tecnología detrás del "Consulta tu caso aquí".
6. Progressive enhancement en toda interacción avanzada (scroll, crossfade): funcional sin JS, respetando `prefers-reduced-motion`.

### Marco técnico preliminar (candidato — no reemplaza 03A)

- **CMS:** WordPress, administrable sin código.
- **Tema:** block theme propio (no Elementor, no plugins de constructor de página).
- **Editor:** Gutenberg nativo, sin modo experimental (AGENTS.md, prohibiciones).
- **Diseño por datos:** `theme.json` como fuente de tokens (color, tipografía, spacing) — no CSS suelto por bloque.
- **Composición:** patterns + template parts reutilizables; **core blocks primero** — bloques custom solo si un core block no alcanza, y con decisión registrada.
- **JavaScript:** mínimo, progressive enhancement, sin frameworks (D7 ya fija esto para scroll/interacciones).
- **HTML:** semántico, accesible por defecto (landmarks, encabezados jerárquicos).
- **CSS:** por tokens de `theme.json` / custom properties, no clases utilitarias ad-hoc ni Tailwind (restricción ya vigente para el prototipo, se traslada al tema).
- **Plugins:** mínimos — ninguno nuevo sin necesidad y decisión registrada (AGENTS.md). CPTs diferidos a 03A (D3).
- **Entornos:** Local → Staging → Producción, repositorio Git privado (ya inicializado).
- **Administrabilidad:** todo el contenido editable desde WordPress, sin dependencia de código para cambios de contenido.
- **Responsive:** mobile-first o adaptado, no solo reducción proporcional del desktop (REFERENTS.md → principios obligatorios).
- **Accesibilidad:** contraste AA mínimo, estados hover/focus/active, `prefers-reduced-motion` respetado.
- **SEO básico:** URLs limpias, metadatos editables, hreflang vía Polylang (ya contratado).

### Contenido/evidencia faltante (consolidado — detalle en STATUS.md → Vacíos)

Casi todo el contenido de Talento AAA; casos de éxito estructurados (Nuestra Experiencia); estatus del párrafo narrativo candidato de servicios; fotografías/logos (confirmados disponibles, no entregados); testimonios de talento (no existen); autoría del blog.

### Preguntas de decisión (para Product Lead)

- ¿Se confirma la página de captación de empresas (D11) como Change Request formal, o se descarta a favor de alojar el formulario en una página ya vendida (p. ej. Capacidad Técnica)?
- Nombre final de esa página (candidatos sin decidir en [content/06-captacion-b2b.md](content/06-captacion-b2b.md)).
- Cuando se comparta la URL externa de ADY (D18), ¿el CTA "Consulta tu caso aquí" y el formulario nativo de empresas (D9/D12) son el mismo flujo, o dos entradas distintas al mismo objetivo? No se resuelve hasta conocer la URL.
- Reparto de los "hasta 3 formularios" entre empresas, Talento y cualquier otra necesidad (contacto general, newsletter).

*(Cerradas en esta actualización: "quiénes somos" — D19, estructura creada, copy pendiente del cliente; Microsoft Clarity — D20, propuesta adoptada.)*

**Estado:** ✅ C1 aprobado (2026-07-23) y ✅ **copy final aprobado (D21, 2026-07-24)** — v1 para Nuestra Experiencia/Talento AAA/Noticias y Análisis, v2 (SEO) para Inicio/Capacidad Técnica/Nuestro Equipo/Captación de empresas. Página Legal (08) sin outline de contenido por regla D2; ADY (09) es especificación técnica. Placeholders explícitos donde falta insumo del cliente (Talento AAA, biografía de founders, casos de éxito) — no aprobados como finales, no inventados. **C2 — prototipo candidato construido (2026-07-29)**, ver [STATUS.md](STATUS.md) → "C2 — Prototipo": `/prototype/` (Inicio + Capacidad Técnica), pendiente de QA de Content y consolidación de Product Lead antes de Gate 1. La página de empresas (D11) no se incluyó por falta de confirmación contractual.

## Definition of Done

Para B00 (inicialización + cierre de decisiones sobre pendientes), el DoD es:
- [x] Insumos inspeccionados en solo lectura (brief, propuesta, brochure, manual de marca, sitio actual, repositorio)
- [x] Estructura documental creada (AGENTS.md, PROJECT.md, REFERENCES.md, STATUS.md, QA.md, REFERENTS.md, .gitignore)
- [x] Contradicciones, hipótesis, vacíos y riesgos hechos visibles (STATUS.md)
- [x] Git inicializado (sin commits — no solicitados)
- [x] Decisiones del Owner sobre los 8 pendientes registradas (D2–D7 arriba; dirección de oficina y autoría del blog quedan explícitamente pendientes, sin bloquear C0)
- [x] Ficha C0 producida y **aprobada** (2026-07-23, ver STATUS.md).

Para el **proyecto completo**, el DoD (a nivel propuesta, sujeto a Gate 2) es:
- Sitio implementado en WordPress, administrable, con hasta 6 secciones + blog
- SEO/GEO + performance verificados
- QA en 3 capas (técnico/visual/funcional) superado
- Gate 2 aprobado por el cliente (funcionalidad validada, hasta 2 rondas de QA cliente)
- 1 ronda de ajustes post-QA cliente consolidada e incorporada
- Capacitación de administración entregada (1 hora)
